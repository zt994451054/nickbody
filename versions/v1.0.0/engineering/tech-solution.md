# v1.0.0 技术方案

> **技术架构基线**：[foundation/tech-arch/overview.md](../../../foundation/tech-arch/overview.md)  
> **需求文档**：[product/requirements.md](../product/requirements.md)  
> **接口设计**：[api-design.md](./api-design.md)  
> **数据库设计**：[db-design.md](./db-design.md)  

---

## 1. 方案概述

本版本在 macOS 上交付 `nick` 产品的首个原生客户端。技术策略采用**纯端侧单机零后端架构**：
1.  **UI 表现层**：采用 SwiftUI + AppKit，通过配置为 `LSUIElement`（无 Dock 图标）的常驻菜单栏项目承载。大厅和跟练窗口按需使用 RealityKit 3D；桌面 `NSPanel` 使用 Core Animation 播放低功耗透明 sprite strip。
2.  **疲劳追踪**：基于全局鼠标/键盘活动事件监视器（非侵入式，无录入风险）。
3.  **姿态识别与领操**：利用系统内置的 `Apple Vision`（`VNDetectHumanBodyPoseRequest`），并以 `PoseProvider` 协议隔离 Vision 依赖。
4.  **角色动画**：Rig v2 使用固定 32 关节生产骨架，表现动作在 Blender 4.3.2 制作并烘焙；运行时由唯一 `PetAnimationGraph` 分层合成最终关节姿势。
5.  **数据持久化**：使用 macOS 沙盒内的 `SwiftData` 本地库。
6.  **变现控制**：使用 StoreKit 2 本地验证订阅状态，无云端校验。

---

## 2. 架构变更

CHANGE-011 引入生产级角色动画架构，保持纯端侧和零第三方运行时不变，但调整宠物资产与动作边界：

- 当前 Meshy 24 骨资产只保留为身份与回归参考；新生产资产遵循 `pet-character-rig-v2.md` 的连续网格、32 关节骨架、手工蒙皮和认证合同。
- CHANGE-024 允许从许可证、归因、不可变 artifact/source-topology hash 均已绑定的人工 basemesh 手工适配生产网格。CHANGE-025 首个 v009 revision 已证明该 source 可显著降低基础自交和折面，但没有施工完整关节环与 support routes，strict geometry 也未通过；它是已冻结的 S0 失败证据，不能替代小草身份或任一 Rig Gate 证据。CHANGE-026 统一合同和 WIP/formal 分层后完成左肩 R0：scoped 静态机器门通过，但一次性三骨与线性测试权重的变形诊断失败，当前 licensed-basemesh AI 施工路线已关闭。该结果只否决本次 R0/临时 rig/测试权重组合，不独立否决 licensed source topology；v002 与 production Rig Gate 0 均未授权。
- Blender 是固定版本的离线 DCC，不随 App 分发。Meshy 只作为候选生成器，不再决定生产 rig 和最终权重。
- 大厅/跟练的 RealityKit 3D clip 与桌面 sprite strip 来自同一已批准 DCC action。
- `PetAnimationGraph` 是关节姿势的唯一最终写入者，取代 `PetSceneView` 和多个 planner 直接拼装骨骼角度的模式。
- 详细决策见 ADR-003；ADR-002 的“移除 Rive、桌面使用预烘焙帧”继续有效，固定整张图集部分由独立 strip + manifest 取代。

---

CHANGE-030（2026-09-08）新增用户已批准的 Tripo vendor capability trial：以现有模型及
H3.1 detailed/P1 四视图对照，验证自动绑定、实际动作和表情可行性；允许诊断格式转换、
贴图/减面和隔离 RealityKit 导入，按整轮推进。此试验不采用供应商骨架替换生产 32
关节合同，不宣称 S0 或生产动画验收通过。范围与数量上限见 Rig v2 §8.0.10。

整轮已完成：两个新模型、三次绑定、十段预设动作，净消费 260 credits，余额 305。
H3.1 外形优于 P1；两份通用 v2.5 rig 动作变形未通过。官方推荐的人形 v1.0 产生
23 个标准命名关节，本地姿势改善但肩髋问题仍在，一次重合顶点清理未修复；服务
明确拒绝 Mixamo retarget。没有得到面部控制。身体筛选未通过，因此未进入条件性
贴图/交付减面/RealityKit 接入，不对运行时架构作变更。

CHANGE-031 已根据用户“继续”完成后续减面和原生人形绑定复测：smart 20k 目标输出
27,595 面但损伤脸/叶细节，改选 basic 80k；人形 v1.0 / Tripo 原生输出 41 骨，
五种 preset:biped 动作全部交付。305 个回放帧、22 个姿势的 44 张双视角图表明
接口链路已成功，但胸腹折痕、肩髋皱缩和远端背部拉扯仍未通过，表情控制仍缺失。
本轮消费 115 credits，余额 190；未追加条件性贴图或 RealityKit，不改运行时架构。
该轮完整记录见 Rig v2 §8.0.11。

CHANGE-032 已完成用户批准的一次本地权重修整。只修改原五动作 GLB 的局部蒙皮
字节，几何、骨架和动作不变；上部中央躯干大腿影响 0.189981386→0，相邻边极端
权重跳变 74→27。305 帧及同源前后共 88 张姿势图显示上胸背改善，但腰髋仍折叠，
转身腰部折痕更集中，整体未通过。副本仅留诊断证据，原模型仍作基准。下一步应
拆查骨盆/大腿/twist 权重过渡及四影响截断，再按证据判断骨位或局部拓扑需求。
本轮无新增 Tripo 请求、0 credits，最后核实余额 190；未做表情/贴图/RealityKit，
不改变运行时架构与正式合同。工具 7/7、行覆盖 88.24%，详见 Rig v2 §8.0.12。

CHANGE-033 按用户授权进行 1000px 放大审查与逐部位修整。连续腰部权重场明显
减轻转身腹部锯齿，保留为加工中间态；背侧臀腿折叠仍在，下一步膝部试修因转身
前侧新折痕未采纳。腰部中间态复测五动作 305 帧、22 姿势 44 图；共有 68 张
局部近景，区域与全身均未验收。下一个局部工作仍是把膝盖驱动的折线映射回静止
表面，检查权重梯度和膝位，不能据当前结果直接认定需要重拓扑。工具 11/11、
行覆盖 92.15%，只证明处理行为；消费 0，无表情/贴图/RealityKit 或合同变更。
详见 Rig v2 §8.0.13 和 App tripo-region-refine-20260908 实验入口。

CHANGE-034 已把膝盖驱动折线映射到 114 个原始三角面、246 个顶点，并完成后侧
权重、临时蒙皮/旋转中心及局部修形对照。直接平滑分离顶点会产生新裂口；临时
连通表面后该裂口消失、跳跃背侧改善，但左右单膝仍有折角且右腿局部退化。
膝部试修全部未采纳，仍使用 CHANGE-033 腰部中间态。下一步先设计左膝受压表面
的目标形状与按角度补偿，验证过渡姿势后才扩到右侧。代理无 UV 转移、无 GLB
交付，不改变运行时架构。52 张近景，工具 8/8、行覆盖 93.10%，0 credits；
详见 Rig v2 §8.0.14。辅助相交数减少不能视作自然变形通过。

CHANGE-035 已制作左膝 0/15/30/45/60° 解析弯曲目标与三种局部边界对照。
最新 harmonic 边界减轻 30–60° 横向折线，但内侧/下缘仍有细褶，
15° 出现局部退化，左膝仍未通过。目标形状只留诊断参考，GLB 加工基准仍为
CHANGE-033 腰部中间态。没有 morph driver、修复 GLB 或运行时交付。
工具 12/12、行覆盖 91.75%，真实模型修后坐标与接手前一致；0 credits。
下一处先验证低角度下缘和边界切向过渡，再看右侧与原动作；详见 Rig v2 §8.0.15。

CHANGE-036 已验证包含相邻固定行的二阶边界位移求解，完整审核五档角度、
两视角、三组共 30 张原图。15° 下缘褶皱和大角度内侧凹折仍在，
相较 CHANGE-035 未证实足够增益，本轮不采纳为加工基准。下缘采样绑定
19 面、30 点，横跨冻结/过渡/目标区；下一处检查目标与固定表面的匹配，
再定义 15° 局部目标和支持区域。工具 5/5、行覆盖 87.56%，不代表形状通过；
0 credits，无修复 GLB 或运行时交付。详见 Rig v2 §8.0.16。
CHANGE-037 [膝部修复计划](./pet-knee-repair-plan.md) 已明确诊断顺序、最多两版
局部目标试修、连续性/交付可行性检查及结构性备选触发条件。

CHANGE-038 已完成 P1 和两版 P2。首版延后生效仍在 25° 产生下缘细褶；第二版
仅下移支持带，15° 下缘最大折角由旧 66.96° 降至 28.18°，60° 由 128.54°
降至 72.59°，内侧凹折仍在。第二版只留形状参考，腰部 GLB 继续作为加工基准。
P2 已用 2/2。内侧 75 面补查区分出源冻结缺陷与补偿放大的过渡折角，下一种
方法选择内侧上缘局部小腿权重重分配，尚未实施；范围与预算见修复计划 §7。
工具 11/11，三个模块行覆盖率 84.25% / 86.32% / 88.72%，与代码哈希匹配。
35 保护项、318 个历史产物与 305 帧未变，消费 0；无修复 GLB、driver、UV 转移、
右膝/动作扩展或 macOS 14 可实现性验证，不改变运行时架构。详见 Rig v2 §8.0.17。

CHANGE-039 已完成内侧上缘局部蒙皮首版与归档。140 个连续代理点对应 183 个修改源点，
三条目标边在 60° 明显改善，但横向折线仍在；转身 47 帧新增 19 对相交，绑定到
20 面、23 代理点、28 源点，全部在编辑区内。首版不采纳，腰部 GLB 继续作为加工基准。
S 已用 1/2、第二次未使用；当前缺少单因素第二版依据，结束比例分配配方，
后续先形成跨姿势压缩表面与固定边界的 DCC 制作方案，详见修复计划 §8。
工具 8/8、行覆盖 100% / 91.87%，当前源码与成功日志一致；原 51 图和补拍 8 图保留，
接手复核其中 22 张。50 保护项、前轮 111 私有产物与 11 工具/文档、此前 318 产物及 305 帧未变。
本轮只生成隔离诊断 GLB，未替换正式资产，未执行 P3/P4 或 macOS 14 交付验证；
新增消费 0，不修改运行时架构、基础设施依赖或生产合同。详见 Rig v2 §8.0.18。

CHANGE-040 完成四姿势牵引分析及可打开的 Blender 检查场景。固定原减重数量、范围及组内比例时，
15°/60° 单改大腿/腰部接收比例的最大位移跨度仅 7.62e-9，转身/跳跃却为 0.0316323/0.0165869。
因此比例补丁不足以修复单膝横折；未否定其他减重场，S 第二次未启动。
10 个静态网格绑定实际选区、20 失败面和 51 相邻冻结点，完整源映射/牵引/面几何单独留存。
20 张 1000×1000 图全部复核，左膝仍未通过；尚缺可接受的四姿势目标表面及统一减重场。
工具 8/8、行覆盖 100% / 98.34%，143 保护输入、历史产物/305 帧及无关路径未变。
无新权重、修复 GLB、目标形状、费用、运行时架构或依赖变更。详见 Rig v2 §8.0.19 与修复计划 §9。

CHANGE-041 完成 D1 四姿势局部目标。630 连续代理点、793 源点，实际边界约束 32 点位移笼，
完整目标/控制数据与 16 张原图已保存并复核。60° 相交 30→0，但台阶和内侧压折仍在；
转身新增 10 对与固定左手的接触，跳跃折痕转移，因此目标组不采纳。
原非零骨集合及固定 rest/bind 的合法统一蒙皮 RMS 误差下界 0.00612566，达到该组目标
所需位移 RMS 的 72.5%；不能精确复现这组目标，不代表其他形状或拓扑不可行。
D1 控制笼办法结束；S2 未启动，P2 2/2、S 1/2 保留。下一项定点造型须处理左手净空、
膝窝台阶及跳跃折痕，见 Rig v2 §8.0.20 与修复计划 §10。工具 10/10、覆盖 97.37% / 98.64%。
181 保护输入、历史产物/305 帧及无关路径未变；费用 0，无新权重、修复 GLB、架构或依赖变更。

CHANGE-042 完成 D2 六笔定点编辑，但目标不采纳：60° 尖折边 13→21，转身原十对新增
手部接触消失却产生七对新大腿自身相交，跳跃折痕仍在。共同蒙皮误差下界达所需位移
73.07%；全部 41 原骨的单膝 y 跨度约 1e-7，不能复现 D2 的 0.004 横移。
结束该脚本点改方案；仍缺可实现的自然目标，由角色制作人员接续造型判断，
确切失败面及调整成本见修复计划 §11 / Rig v2 §8.0.21。S2 未启动，P2 2/2、S 1/2。
工具 6/6、覆盖 96% / 100%；229 保护输入与历史证据未变，0 credits，无架构或依赖变更。


CHANGE-043 重排计划已由 CHANGE-044 实施：源验证/共同方向和一个联合权重方案通过准备，
随后在 1,224 源点上执行唯一一次 S2，200 外迭代到达上限；S 2/2、P2 2/2。
60°/转身/跳跃尖折边 142→44、136→17、103→28，但新增相交 5/1/8 对，
24 张原图仍见横折和内侧折回，S2 拒绝，未进入连续动作、右腿和交付验证。
真实 Blender/预测误差 1.45955e-7，源几何/UV/rest/动作、接缝和范围外权重保全通过；
14/14 工具测试、四模块行覆盖均 >95%，源与正式资源未替换，无新依赖或费用。
当前平均折角目标及试后碰撞筛查未达到要求；达到迭代上限不是收敛或不可行证明。
CHANGE-045 已完成该实施单及只读清单：3,398 条可变折角与 310 条全冻结折角分开，
跳跃有 7 条冻结尖折边；184 个涉及可编辑面的姿势相交对先恢复，再优化最坏折角，
每步连续碰撞保护原分离/已恢复对。95 个输入前后核对一致，未生成新权重或实现新求解器。
CHANGE-046 原图定位已确认七条固定边形成必须处理的可见内侧折回，原范围与目标冲突。
单一连续连接带提案增加 41 代理点 / 65 源顶点，总计 1,039 / 1,289，右侧、左手和上腹仍冻结。
提案比较范围内的未改源恢复清单为 188 个姿势相交对，固定上下文 24 对；新增三个固定尖折均在右侧。
28 张图已归档，4 张主视图与原图逐像素一致。App `e20016f` 的遮挡、范围和约束数学工具
18/18 测试通过，三模块覆盖 95.73% / 97.80% / 100%；369 保护输入和 4,725 无关路径未变。
CHANGE-047 已实现原支持共享变量、QP/CCD 适配、R/F 状态机及退出审计，41 项不同测试通过，
六模块覆盖 96.20%～100%。源/S2 静态和历史完整更新路径回归通过；38,961 冻结点雅可比为零。
App `bc26252` 冻结实现及参数后执行单次 C1：首个 R 子问题达到 5,000 内迭代上限，
`qp_failure`，1 次尝试、0 接受步、18.108 秒，没有生成替换权重或进入造型与连续性验收。
只读首个 QP 的零步违反量 2.220446049e-16；当前失败不证明不可行，数值尺度原因尚待验证。
CHANGE-048 已补齐失败向量、原单位残差和等价表示，27 项合成测试通过；
固定 Q1 中原 OSQP 配置再次超限，Clarabel 在 30 内步 / 2.226 秒通过同一子问题。
独立原单位 primal / dual 为 5.915431190e-18 / 1.444714593e-13，原目标/对偶下界间隙 2.058461154e-11。
第一固定 QP 可解；CHANGE-049 已继续接入完整 R/F，29 项测试通过后执行一次 C2：
23 QP 全部 Solved、23 接受步 / 901.502 秒，在 R 停滞；最大恢复松弛下降 33.1056% 但未归零。
Blender 原支持/接缝/静态数据/动作不变量通过，20 张原图造型拒绝，F/D 未进入。
追加固定子特征与快照验证 10 项测试通过，发现 6 处坐标不变的边穿过冻结左手面，
精确有理数及 Blender 源坐标复核成立，证明当前冻结范围的零松弛恢复目标不可行。
仅补边界 10 点仍不充分，原 Hip 高度边界和手部冻结有直接冲突；
下一项重建膝—髋接触区域与有界保护边界提案，见修复计划 §18 / Rig v2 §8.0.28。

CHANGE-050 已完成上述只读预检：全源五姿势 2,563 相交对、5,126 严格见证经三种坐标核对。
最终范围提案 2,189 代理点 / 2,690 源点，保留旧膝区，显式解除旧整臂保护的 925 点，
保留掌指/右侧和静态不可达接触面冻结；1,500 个恢复对包含全部旧 188 对。
新增 1,150 点的位移上限/过渡约束已准备，原膝折角门槛保持，新增接触区按逐边源值保护。
31 项测试通过；必要预检通过不证明共同可行或自然造型。该轮未生成新权重或登记 C3，
范围与保护提案保留于修复计划 §19 / Rig v2 §8.0.29。

CHANGE-051 已接入上述完整 R/F 与有界保护，40 项测试、真实源组装/导数/静止 CCD 通过。
独立 C3 在 R 接受 7 步后超时；8 个 QP 均精确 Solved 且原单位 KKT 通过，但最大恢复松弛
仅下降 0.537824%，1,500 对源相交全部残留。实际 1,833.035 秒，末次同步核验跨过 1,800 秒
截止点，未在截止后接受步或新开 QP。40 张 Blender 原图造型拒绝，F/D 未进入。
同操作顺序原权重对照后，新增区转身/跳跃折角仍超限 0.000163800 / 0.000095192 rad，
说明仿射状态保护通过尚不能保证 Blender 实际保护；未放宽 1e-6 门槛。
该轮的非线性折角与实际精度问题保留；其首个“临近接触”分类已由 CHANGE-052 更正，
历史记录见修复计划 §20 / Rig v2 §8.0.30。

CHANGE-052 定位并修复严格源接触漏检：小三角面 Gram 行列式低于旧固定阈值，
导致原相交误判成分离。新隔离筛选采用向外舍入与精确回退，三套源坐标接触 ID 一致，
当前范围补回 34 条，恢复清单增为 1,534；原支持、范围、位移和折角门槛保持。
49 项测试与新源 R/F 完整组装、导数通过；同一冻结方向的 CCD 安全比例约增至 6.45 倍。
C3 终点在仿射与 Blender 中仍保留全部 1,534 条相交，没有新增/移除，模型未修好。
模拟 float32 权重和坐标仍会漏掉实际保护失败，Blender 验收继续必需。该轮结果保留于
修复计划 §21 / Rig v2 §8.0.31，当轮 K1 尚未登记。

CHANGE-053 完成新增接触可达性必要条件及共面/边界复扫，1,534 条清单不变；
新完整 R/F 接入稳定 ID、提前拒绝、非线性修正及共用预算，95 项新旧测试通过。
真实源准备通过后登记唯一 K1，最多 3 修正 / 600 秒；一次原生修正精确 Solved，
真实硬违反降至 2.9892e-11，完整路径检查通过，最大松弛改善 0.428653%。
同一权重在 Blender 中实际分离 4 对原相交，间隙达标且无新增相交；但转身/跳跃
共 113 条保护折角失败，最大超限 0.000180014 / 0.000392298 rad，候选拒绝。
原源对照、冻结/静止/接缝坐标及位移盒通过，下一项针对固定失败顶点验证实际求值差异，
再接入 Blender 保护检查，见修复计划 §22 / Rig v2 §8.0.32。K1 已用 1/1，不自动 C4；
模型仍未修好，没有整体无解证明。
P2 2/2、S 2/2、C1/Q1/C2/C3 各 1/1；试修权重仅存隔离诊断，未替换源或正式资源。
426 保护输入及 4,725 无关路径未变。离线 OSQP 1.1.3 / IPC Toolkit 1.4.0 等依赖已在隔离环境安装，
版本/哈希、许可证及上游构建配方已登记；运行时架构、基础设施和生产合同不变。
CHANGE-048 新增 Clarabel 0.11.1 / cffi 2.0.0 / pycparser 2.23 独立诊断环境与许可证记录，
539 历史文件、565 登记绑定和 4,725 无关路径核对不变，旧 C1 源码和环境未修改。

CHANGE-054 逐层捕获权重、矩阵和局部/世界/骨架坐标，源/K1/C3 五姿势全部精确复现。
去掉世界坐标绕路不能消除 K1 的 113 条失败；归一化内部矩阵的双精度表达仍漏掉其中 111 条。
独立候选搜索新增强制 Blender 实际保护，在数学路径通过后、收益和接受前执行。
权重包/二进制/代码哈希、同操作源对照、独占输出和共享超时均校验；1e-6 与严格位移盒保持。
72 项新旧测试通过，216.486 秒的实际回归中源通过、K1/C3 的 113/84 条失败拒绝。
完整 R/F 尚未使用新入口重跑，门禁不替代实际接触、连续性或原图验收。
下一项按修复计划 §23 / Rig v2 §8.0.33 完成 K2 固定段回退驱动与登记；
当前只有四候选 / 900 秒 / 零 QP 的提案，未登记或执行。模型仍未修好，无整体无解证明。

CHANGE-055 完成独立 K2，四比例在 61.339 秒内全部提前拒绝、已用 1/1。
唯一失败为转身面 57223/57352 的新增区折角；最小步仍超限 1.52805e-6 rad。
源和 K1 的数学端点通过，长双精度确认该方向的非线性折角变化，不是辅助状态或舍入误报。
47 项测试通过；本轮没有 QP、Blender 或实际接触复扫，不能推断实际恢复改善。
围绕最小冻结步已准备完整局部修正问题，保留全部约束、1/4 独立步幅半径及收益上界。
下一项按修复计划 §24 / Rig v2 §8.0.34 完成 K3 驱动与单次登记；K3 尚未登记/执行，
最多 3 修正 / 600 秒后仍须过实际保护与接触/收益检查。模型未修好，不自动 C4。

CHANGE-056 完成 K3：1 次修正与 1 次真实 Blender，264.705767 秒内实际保护拒绝，已用 1/1。
55 项新旧测试通过；源/中心及完整原单位 QP 精确复现。
数学硬违例 1.5901058248e-11，仿射 CCD、面积路径、静态接触、固定界和保留间隙通过；
实际转身 88、跳跃 81 条保护折角超限，最大 0.000174932638 / 0.000155311928 弧度。
源/冻结/rest/接缝误差 0，实际盒通过；实际接触和收益验收未执行，模型仍未修好。
169 行只读定位完成。原 K2 阻断行数学已过、实际仍超限，下一项按修复计划 §25.4 / Rig v2 §8.0.35
先准备实测坐标锚点预测并保留全部原约束，再独立登记单次 K4；当前未准备/登记/执行。
预测并非等价 Blender 或误差界；实际门禁不省略，不重启 K3、不自动 C4，无整体无解证明。

CHANGE-057 K4 完整实测增广问题为 150,940 行 / 13,578 变量，实测锚点及导数核对通过。
唯一 QP 在 41 内步返回 PrimalInfeasible，72.879620 秒停止、已用 1/1；无候选/Blender。
86 项新旧/审计测试通过；精确证书证明原零残差线性问题在固定盒内不可行。
跳跃面 70418/70988 残差 9.26320e-7，已在既有 1e-6 容差内，却被要求归零，
其最小线性残差仍为 4.11930e-7；不能据此定性实际修复无解。
独立编译器已对齐两个折角族的已有容差，34,068 行调整、其他 116,872 行及允许域保持。
K5 完整初始 QP 已准备但未求解，旧证书不再适用不代表新问题已可行。
下一项见修复计划 §26.4 / Rig v2 §8.0.36：完成 K5 驱动与登记，再作有限非线性修正及真实门禁。
K3 实际 169 行失败仍未解决，模型未修好，K4 不重启、不自动 C4，D 未启动。

CHANGE-058 K5 完成驱动、输入复现、独立登记和执行，111 项新旧测试通过。
首个 QP 46 内步返回 PrimalInfeasible，80.543966 秒监督停止，已用 1/1；没有候选或 Blender。
在既有折角容差及固定 K2 允许域下，冻结线性问题的精确分离量为 1,006,408.6180969396。
只读进一步定位 12 行原数学/实测折角充分冲突子集（5 原、7 实测；转身/跳跃各 6），
分离量 1,388,451.765436459；其他接触、面积等行无需参与这一证明。
这不是最小冲突或非线性/实际无解结论。模型未修好，K3 的 169 行实际失败仍未解决。
下一项见修复计划 §27.4 / Rig v2 §8.0.37：只读审计对应面片的坐标、法线、导数及非线性余项，
找出两套预测差异再准备新提案。K6 未登记，不重启 K5、不自动 C4，D 未启动。

CHANGE-059 只读审计完成：12 行映射到胸肩/左上臂支持的 8 顶点、9 面与 24 变量。
骨矩阵完全相同，坐标偏差随源/K3 状态改变，角点一阶解释的最大剩余误差为 2.02844e-8 弧度。
局部源权重在投影盒内且真实/原数学源折角通过，K3 锚点预测却误报 6 行；
完整源的 652 变量（含 35 权重）不在全域盒内，不能据此宣称完整修复可行。
所需平均向下非线性余项至少 2.26310e-5 弧度，3 行无可用估计界；无实际无解证明。
16 个简单算术对照未复现真实残差，待隔离网格→世界→骨架采集往返。
直接 float64 网格→骨架坐标函数已测试，73 项测试通过，本轮无候选/QP/Blender。
下一项按修复计划 §28.4 / Rig v2 §8.0.38 完成 K6 配对采集驱动与登记；
最多 1 次 Blender / 600 秒，只用冻结源/K3，先复现旧路径后比较两种采样。
K6 未登记/执行，原真实门禁不替换，不自动 C4，D 未启动，模型未修好。

CHANGE-060 K6 已在 50.5160715 秒内以一次 Blender 完成冻结源/K3 的同快照配对采集，已用 1/1。
旧路径全部身份与原保护精确复现；直接路径的失败集合仍为转身 88、跳跃 81，严格位移盒均通过。
12 行簇的运动偏差往返贡献为 0，折角残差贡献最多 4.23550e-12 弧度，剩余偏差仍为 1.75199e-4。
已知源预测误报仍为 6 行，更换采集器不能解决当前失败；模型未修好，无整体不可行结论。
59 项测试通过，无新权重、QP 或导出；独立 NumPy 末位差异的失败证据保留，随附 Python/NumPy 只读重放后精确通过。
下一项按修复计划 §29.4 / Rig v2 §8.0.39 只读审计全量失败保护点的源权重保留条件和固定域/间隙耦合。
K7 未登记，不重启 K5/K6、不自动 C4，D、全部原门禁与正式资源保持。

CHANGE-061 完成整体路线评审和源权重保留必要条件核查；模型未改善，169 行实际失败保持。
全部失败涉及 282 个规范点，7 点的原权重在当前固定域外，部分变量与保留间隙耦合；
不能把恢复源权重当作已可实施修复，也不能把局部限制外推为整体无解。
后续以路线评审 §5–7 / 修复计划 §30 / Rig v2 §8.0.40 为准，停止滚动追加 K 诊断。
当前纯权重路线共享 300 分钟主动工作、最多一次新试修，依次为完整方案 60 分钟、
实现与完整实际局部验证 120 分钟、条件性连续动作与交付 120 分钟；失败即切换路线选择。
当前 41 骨试验到正式 S0/32 关节生产资产的适配须在方案中交代，原认证合同不变。
本轮无新候选、QP 或 Blender；R1 未开始，K7 未登记。

CHANGE-062 执行 R1 后未通过：源点固定会保留目标接触，保留可动点的方案仍依赖未解决的真实求值误差；
源基准预测在已有 K3 上误放行六处实际失败，不能直接进入唯一一次试修。
按路线评审 §9 关闭当前纯权重路线自动投入，R2/R3 未启动，下一项转结构方案选择。
精确范围、穿越见证、预算结算和生产适配成本见 R1 方案结论；原门禁不变，本轮没有模型改善。

CHANGE-063 完成结构比较，优先选择一次有界局部定向拓扑施工，未启动新模型。
骨位移动已有未通过证据；膝部与肩臂小补片联合会漏掉 1,280 对恢复目标。
完整包络已固定为 4,906 原面、七边界/484 条边，覆盖全部 1,534 对目标接触，
右侧/掌指冻结点均保持在边界；连接和覆盖只是施工必要条件，未证明可修好。
下一项按结构方案 §5–6 开始 B1：静态网格 120 分钟、一套权重及实际四姿态 180 分钟、
条件性连续性和隔离往返 60 分钟，最多一个候选；连同比较阶段总上限 420 分钟。
B1 0/1；当前权重路线保持关闭，旧额度不转入。41→32 骨权重/动作和正式认证仍须另做。
本轮无新网格/权重/QP/Blender，无模型改善；原正式合同和运行时架构保持。

CHANGE-064 执行 B1 静态施工：首次构造前补全关节核心及一圈范围为 5,824 原面、九边界/538 边。
一次固定点位的换边/配对产生真实 Blend，保留 74,176 外部原面、全点坐标、来源及 UV 接缝。
独立审计拒绝：856 个局部三角面、8 个折叠四边面、1 对新增静态自交，四个关节的合格闭环均为零。
双向表面距离超过事前固定 0.001 上限，原尺寸图也有新增裆部角痕；具体数字及哈希见结构方案 §9–10。
B1 1/1 已用且关闭，后续 180/60 分钟不开放；没有新权重 QP、权重、动作或正式资源替换。
本次有失败实物与完整直接证据，没有可采用的模型改善。固定点位局部换边未能完成整体面流，
不能等同于允许重排内部点的完整定向重拓扑已失败。下一项只做生产路径决策，原架构/依赖/32 关节合同保持。

CHANGE-065 已完成[完整重建决策与 DCC 工单](./pet-character-production-rebuild-decision.md)：
选择以正式身份为造型依据，直接制作完整身体的关节/分支面流，再按原 S0→闭合身份→用户审核→
32 关节与 Gate 0–4→格式/运行时往返顺序交付。Tripo 41 骨只保留试验/运动参考，不能直接晋级。
CHANGE-065 当时没有实际完整布局与已确认建模执行能力，未启动新自动施工；建议 DCC 建模协作补足，
AI 继续负责合同、审计及接入，未联系或购买服务。旧 licensed 原件临时路径已失效，官方恢复返回 403，
还须恢复同 hash 文件。无新模型/权重/动作，无新额度，原架构/依赖和正式合同保持。

CHANGE-066 承接用户无法寻求人类协作、授权 AI 继续验证的指令，完成
[真实肩腋参考与迁移设计](./pet-character-shoulder-migration-study.md)。合成肩部三组修复结果已有
联合质量证据，该阶段真实身份源量测形成 8,545 面参考区及五个独立手臂截面，当时尚未生成真实新四边补片。
随后 CHANGE-067 按该设计 §4 实施：源图通过，生成并审计 480 点 / 432 四边面首版。
单连通、冻结边界和无自交通过，双对角线折面 3/11、最大边长比 24.40685、贴合及支撑成带未通过，原尺寸观感拒绝。
本次完整布局门未成立，首版 1、修正版 0、优化 0 后结项；15 项工具测试通过，下一项先完成连续支撑带、胸背极点净空及端口缓冲的实际连接预检，详见设计 §6。
原 B1、权重和旧 builder 不重开，本次未授予后续新施工额度。
该研究不改变正式 licensed 来源合同、身份/骨架、工具依赖或 App 架构，完整角色生产仍阻塞。


CHANGE-068 进一步先完成支撑连接预检，再制作 1,024 点 / 976 四边面的真实补片。
两组实际连续支撑带、原核心及真实一圈极点净空通过；但自交从 0 增至 37 对，折面从 13 增至 182 个，
双向最大贴合误差也增加，实物仍拒绝。参数中点缓冲没有达到精确 `.195` 截面，偏差已披露。
19 项工具测试通过；一设计/一实物/一审计，零修正/优化后关闭，详见[迁移设计 §7](./pet-character-shoulder-migration-study.md)。
下一项验证布点方法，不能将连接进展称为模型整体改善；正式合同、架构与资源不变。

CHANGE-069 保持同一连接图，完成精确 `.195 m` 缓冲和一次有界源表面布点。
折面从 182 减少到 91，但自交从 37 增至 143 对、核心一圈极点从 0 增至 10、反向贴合最大误差增至 18.92383 mm，
实际网格拒绝，没有整体模型进展。26 项工具测试通过；当前加权配置不追加试跑。
下一项先确定源曲面保护/转换分区及不交叉边界，再联合设计连接和布点，详见[迁移设计 §8](./pet-character-shoulder-migration-study.md)。
正式合同、资源和架构保持。

CHANGE-071 已将独立四边模板接入六块固定接口分区的预检；256 点保持，支撑图与原核心一圈极点净空通过。
但源几何有 18 个对角线法向冲突面、最大边长比 45.79230，源三角覆盖及不交叉边界未认证，未进入新 Blend/独立验收。
详见[方法研究 §5](./pet-character-quad-method-research.md)，下一项先建立真实曲面度量下的有效局部分区与映射。


CHANGE-072 已通过真实源度量辅助图与八区逐源三角覆盖，唯一 1,264 点 / 1,216 四边面保留 256 个原固定点并完成独立审计。
相对 CHANGE-069，自交 143→13、不同折面 91→14、核心一圈极点 10→0；补片→源贴合通过。
但反向最大 4.17172 mm 仍超限，最大边长比 12.42234→28.63180 退步，整体仍拒绝。
下一项先验证固定边界拐角覆盖能力，再设计窄区边数与腋下走向，详见[方法研究 §6](./pet-character-quad-method-research.md)。
50 项测试通过；原生产合同、资源、运行时架构及基础设施依赖保持。

CHANGE-073 完成固定边界覆盖诊断：全部 35,460 个原审计源采样在原 64 条近端参数边内侧都有 ≤0.88005 mm 的源点，逐点重建与归属通过。
这是源点覆盖空间，未证明完整四边布局，也未改变 CHANGE-072 的实物失败。四点角点边界方案只作未应用备选。
下一项保留原 256 点，设计拐角内部导向和窄区按真实宽度分配的连接，详见[方法研究 §7](./pet-character-quad-method-research.md)。
本轮零新模型/Blender/优化，59 项工具测试通过；生产合同、资源和运行时依赖不变。

CHANGE-074 唯一 764 点 / 716 四边面实物保持原 256 点：自交 13→3、最大边长比 28.63→10.50，
但折面 14→15、保护区极点 0→1，补片→源最大误差 1.57→3.21 mm，反向超限样本 34→1,385，整体及原图拒绝。
本轮遗漏完整物理预检停止条件的偏差已披露，工具补上导出前物理拦截，73 项测试通过；没有网格修版或第二次 Blender。
局部模板加调和布点方法关闭，不继续调边数/锚点/行密度；替代构造须先展示同时满足硬约束的可运行证据。
298 项保护文件及正式合同/资源保持，完整模型仍未完成。详见方法研究 §8，App 提交 `41843ac`。

CHANGE-075 完成三类外部方法的有界核查，详见[方法研究 §9](./pet-character-quad-method-research.md)。
Gmsh 的固定实体能力待实测；Foldover-free maps 的已读入口是平面三角/体四面体；Mesh-Untangling 的受支持 CUDA 入口不适用当前 Mac，其他联合约束接口亦未核实。
没有方法获准真实网格构造，本轮无模型改善。下一项仅为一次 Gmsh 独立小样的保留实体能力检查，尚未执行；失败即淘汰，通过也仅进入真实源适配设计。
本轮仅文档变更，无新依赖、架构或运行时代码；347 项保护文件及 App `41843ac` 保持。研究阶段超时 347 秒已披露，总工时及推送见私有本轮回执。

CHANGE-076 实测 Gmsh 4.15.2 单次小样：104 个固定坐标仅 10 个保留，原 64 面/56 段连接均未保留，接入方式关闭。
固定检查失败后未执行完整几何/贴合审计；12 项纯检查器测试通过，App `6004e40`，详见[方法研究 §10](./pet-character-quad-method-research.md)。
Gmsh（GPLv2+）只在隔离本机环境执行；NumPy 2.4.3 为计算依赖，SciPy 1.17.1 已安装但最终工具未使用。来源、wheel/库指纹和许可证证据已登记，第三方二进制不进入 App 或分发物。
沿用 ADR-003 离线实验边界，不采纳为生产工具或增加运行时/基础设施依赖；基线同步记录已结项环境，无新 ADR。下一项仅设计固定块与空缺域分离的构造接口，不继续真实网格。

CHANGE-077 完成固定块与空缺域配对设计，详见[方法研究 §11](./pet-character-quad-method-research.md)。
旧 CHANGE-072/074 保护区域内部仍有 6/9 个折面；固定块隔离和接口保持可以设计，真实合格核心与联合合格匹配尚无依据，不能启动构造。
下一项先解决合格核心块的自动构造来源，暂停外围补洞和工具轮换试跑；普通三角面完全匹配不提供保护区四价保证。
本轮零新网格/求解/Blender/付费，无模型改善；App `6004e40` 与 427 项保护文件保持。只读资料与设计，不引入新生产工具、依赖、架构变更或 ADR。

CHANGE-078 形成[核心联合约束设计](./pet-character-core-constraint-design.md)：160 面固定环带候选＋原源候选连接，联合表达覆盖、实际一圈四价、相交与贴合。
原源三角数/边界离散不相容，固定环跨旧接缝，尚无合格底网或联合解；下一项只做一次相容输入预检，不直接启动四边模型或 MILP。
CGAL 固定发行包和 SciPy 已有封装仅做源码核查，未安装/编译/运行新依赖，不增加 App 运行时、基础设施或 ADR。本轮无模型改善，441 项保护文件及 App `6004e40` 保持。

CHANGE-079 的[核心输入预检](./pet-character-core-constraint-design.md)在全环带平面图中发现 3 处固定边交叉，未调用三角化。问题位于固定带末端，不代表原三维自交或核心无解；下一项先做自由核心/固定三维环带域分离核查。
CGAL 6.2.1 和已有 GCC/Boost/GMP/MPFR 只用于本机编译准备，原生入口最终被 Xcode 许可拒绝阻塞；来源/许可已同步架构基线，无新 App 运行时、基础设施或 ADR。16 项 Python 检查器测试通过，463 项冻结文件保持，无模型质量改善。

CHANGE-080 完成[域分离设计与接口核查](./pet-character-core-constraint-design.md)：固定 160 面不进入自由核心平面生成器，存储核心边界 98 边无交叉，公共环每点需要恰好一个新增邻点。原源接缝对应与有限双侧查询支持该设计，新切缝逐三角来源尚未认证；下一项只做一次只读源域追踪，不进入三角化或联合求解。零新模型/编译/依赖，无模型质量改善；App `5dd5b9f`、原合同和正式资源保持，不改变架构基线、基础设施或 ADR。

CHANGE-081 的[单次源域追踪](./pet-character-core-constraint-design.md)完成新切缝的 65 个源三角、64 次跨边对应，在近端第一段的保守切换检查停止，其余 95 段未测。源域仍未认证，下一项补原边界顶点事件表示；不通过调容差重跑。App 新增只读区间/来源工具及有界证据，最终 17 项工具测试通过，真实输入仅一次且保持拒绝。零底网/编译/求解/新依赖/Blender/付费，不改变架构基线或生产合同。

CHANGE-082 的[原源边界事件与接续核查](./pet-character-core-constraint-design.md)完成剩余 96/96 段，96 个固定连接点通过，复用 CHANGE-081 新切缝；无失败或未测段。最大固定点绑定误差数值上界 0.000015178594 mm，当前 31 项工具测试通过。边界来源阻塞已解除，完整自由域/底网/四边可行性未认证。下一项是既定原生入口及一份核心输入准备，前置通过才最多一次三角化。零新模型/编译/求解/依赖/Blender/付费，未改变基础设施、架构或合同。

## 3. 向后兼容性

本版本为 v1.0.0 创世版本，不涉及用户数据迁移。角色资产采用并行迁移：Rig v2 完整认证和用户批准前，App 继续使用当前正式 USDZ 与整张桌面图集；新资源以不可变版本和哈希并行加入，切换失败时回退旧资产。旧 24 骨 clip 不复制到 Rig v2，新骨架动作必须从 DCC 源重新烘焙。

---

## 4. 新增三方依赖

本版本**彻底实现了零第三方动画运行框架依赖 (Zero Third-Party Animation Runtime Dependency)**。

App 运行时只使用系统框架：RealityKit 负责有界 3D，Core Animation 负责桌面透明帧播放，不引入 Rive、Unity 或 Unreal。Blender 4.3.2 仅用于离线资产生产，不是 App 依赖。CHANGE-024 的 CC BY basemesh 同样是离线内容输入，不是软件包或运行时依赖；若衍生资源发布，须随发布 manifest 保留归因、许可链接和修改说明。该边界消除了 C++ 动画运行时在 macOS 常驻后台时的额外发热风险，并保持最小运行时依赖面。

---


## 5. 配置变更需求

无环境变量。所有配置（如用户疲劳阈值偏好）通过 SwiftUI 的 `@AppStorage`（即本地 `UserDefaults`）进行扁平化存取。

---

## 6. 非功能需求技术实现

| 需求类型 | 需求指标 | 技术实现方案 |
|:---|:---|:---|
| **姿态推理性能** | 领操跟练时推理帧率 `≥ 15 fps` | 在 `AVFoundation` 视频流代理中，使用专有串行后台队列（`Serial Queue`）派发 Vision 请求。配置 Vision 以优先调用系统的 Apple Neural Engine (ANE) 进行硬件加速，防止阻塞主线程（Main Thread）。 |
| **后台功耗控制** | 宠物待机常驻时 CPU 占用 `< 1%` | 1. 桌面常驻只播放预烘焙 strip，不创建 RealityKit 场景。<br>2. 贴边隐藏、全屏工作或静止帧期间暂停 Core Animation。<br>3. 闲时动作由 3–8 分钟随机调度器唤醒，非活动时不运行逐帧 Swift 定时器。 |
| **角色变形质量** | 正式动作无裂缝、远端拉扯、穿模和脚底滑动 | 生产 rig 依次通过结构、关键单关节、全关节、组合姿势和动作逐帧门禁；每个姿势/帧使用八方位 RealityKit 原尺寸图人工审核，机器指标只作问题定位。 |
| **摄像头隐私** | 帧仅在内存处理，永不上传/落盘/入日志 | 使用 `CMSampleBuffer` 直接交给 Vision 提取骨骼点，整个链路无任何写盘（File I/O）动作。Vision 导出的骨骼坐标存入内存数组后，视频帧即被 ARC 机制销毁，日志模块（`os.Logger`）严禁输出视频相关数据。 |
| **数据安全** | 数据不出设备，免 GDPR/CCPA 合规风险 | 全部用户历史跟练和段位记录均存储在 macOS 系统的 Sandboxed 文件夹下的 `SwiftData` 容器中，天然物理隔离，无网络出站请求。 |

---

## 7. 模块设计

```
  ┌────────────────────────────────────────────────────────┐
  │                   macOS Swift App                      │
  │                                                        │
  │  ┌──────────────┐   ┌──────────────┐   ┌────────────┐  │
  │  │ Fatigue-     │ ➔ │ Reminder-    │ ➔ │ NSPanel +  │  │
  │  │ Tracker      │   │ Scheduler    │   │ SpriteStrip│  │
  │  └──────────────┘   └──────────────┘   └────────────┘  │
  │                                                        │
  │  ┌──────────────┐   ┌──────────────┐   ┌────────────┐  │
  │  │ AVFoundation │ ➔ │ PoseProvider │ ➔ │ Scoring-   │  │
  │  └──────────────┘   └──────────────┘   │ Engine     │  │
  │                                         └────────────┘  │
  │  ┌──────────────┐   ┌──────────────┐   ┌────────────┐  │
  │  │ Animation-   │ ➔ │ PetAnimation│ ➔ │ RealityKit │  │
  │  │ Library      │   │ Graph       │   │ Scene      │  │
  │  └──────────────┘   └──────────────┘   └────────────┘  │
  └────────────────────────────────────────────────────────┘
```

### 7.1 疲劳追踪模块 (FatigueTracker)
*   **实现思路**：  
    在后台运行一个独立的 `FatigueTracker` 单例，通过 Cocoa API 的全局监视器来捕捉系统的用户活动事件：
    ```swift
    // 全局监视器，无需 macOS Accessibility（辅助功能）特权，仅检测活动发生
    NSEvent.addGlobalMonitorForEvents(matching: [.mouseMoved, .keyDown]) { _ in
        FatigueTracker.shared.reportUserActivity()
    }
    ```
*   **控制逻辑**：
    *   `reportUserActivity()`：当捕获到活动时，刷新“最后活动时间”。
    *   `FatigueTimer`：每隔 1 分钟检测“最后活动时间”。如果当前时间与最后活动时间差 `> 5 分钟`，则判定用户已离开电脑，暂停疲劳累计计时。
    *   一旦恢复活动，则继续累计。当累计时间突破偏好设置里的阈值（如 60 分钟），向通知中心发布广播 `Notification.Name("FatigueThresholdReached")`。

### 7.2 提醒调度模块 (ReminderScheduler)
*   **实现思路**：  
    监听 `FatigueThresholdReached` 广播。
*   **交互控制**：
    *   收到信号后，向桌面宠物发送 `yawn` 或 `sleep` 语义动作命令，由 manifest 定位独立帧资源，不依赖图集行号。
    *   同时，在屏幕中央淡入（Fade-in）弹出提醒邀请卡片窗口。
    *   **延迟按钮限制逻辑**：在内存中维护 `delayCount`。每次点击延迟，卡片淡出，并在 5 分钟后重新分发信号。若 `delayCount >= 2`，则直接通过 SwiftUI 控制隐藏延迟按钮，仅向用户提供 "Let's move" 的主按钮。

### 7.3 姿态识别与跟练 (PoseProvider & Vision)
*   **实现思路**：  
    定义抽象接口 `PoseProvider` 协议，提供反应式数据流绑定，解耦 Vision 框架：
    ```swift
    protocol PoseProvider: ObservableObject {
        var jointPublisher: Published<UserJoints?>.Publisher { get }
        func startCapture()
        func stopCapture()
    }
    ```
    实现 `VisionPoseProvider`。内部封装 `AVCaptureSession`，设置分发队列。在 `captureOutput` 中将视频帧直接交给系统的 `VNImageRequestHandler`，绑定两个检测请求：
    *   `VNDetectHumanBodyPoseRequest`：提取左右肩关节及头部的 2D 坐标（归一化为 0.0 - 1.0 的 CGFloat）。
    *   `VNGeneratePersonSegmentationRequest`：提取用户人像的遮罩（Mask），用于在影子面板渲染纯色的人像剪影，保护隐私。

*   **跟练角色分工**：左侧 `PetSceneView` 始终按 `PersistenceController.activePet()` 加载用户当前宠物并播放教练动作；V1 当前电子宠物使用之前的 3D 人形小草 `pet_sprout_s2.usdz`。右侧 `MirrorSceneView` 只消费 Vision 关节和头部姿态，固定加载独立的闭嘴狐狸 `mirror_avatar_fox.usdz`；旧张嘴狐狸 `mirror_fox.usdz` 不再进入 App bundle。
*   **资产坐标与静止姿态**：迁移期人形小草仍对当前正式资产保留历史 Z-up 到 Y-up 转换；闭嘴狐狸已统一为 RealityKit Y-up。Rig v2 发布资产必须直接使用 RealityKit Y-up，不再叠加历史根旋转。小草自然收脚站姿由 `idle-neutral` clip 提供，bind pose 保持蒙皮用 A-pose；用户替身继续使用自己的自然站姿回退。

### 7.4 评分引擎 (ScoringEngine)
*   **实现思路**：  
    评分引擎订阅 `PoseProvider` 导出的 `UserJoints` 坐标。
*   **坐标系归一**：Vision 前置摄像头输出的是**镜像坐标系**，`roll` 与 `pitch` 的正负号与用户实际动作方向相反（`yaw` 一致）。该差异在 `PoseTracker` 边界处由 `HeadPose.fromVision` 统一取反，**下游一律使用动作坐标系**（负 = 向左／向下）。此前未做归一时，六个动作中有四个方向判定是反的。

*   **目标角度的标定口径**：
    目标角**由推导得出，不是逐个手填**。此前六个动作的目标角各自写死，导致相对难度严重不均——侧倾要求约为正常活动度的 70%，而仰头只要求约 30%，同一组动作里难易差了一倍以上。

    | 动作 | 轴 | 成人活动度常模 | 目标角（常模 × 0.55） | 满分线 |
    | --- | --- | --- | --- | --- |
    | 左/右侧颈拉伸 | `roll`（侧屈） | `45°` | `±25°` | `±17°` |
    | 左/右转头 | `yaw`（旋转） | `80°` | `±44°` | `±36°` |
    | 低头拉伸 | `pitch`（前屈） | `45°` | `-25°` | `-17°` |
    | 仰头拉伸 | `pitch`（后伸） | `45°` | `+25°` | `+17°` |

    1.  **常模取值**：采用通行的成人颈椎主动活动度参考值，**不得由任何单个用户的实测数据反推**——个人极限幅度只能证明目标可达，用它抬高全局阈值会影响其他用户。
    2.  **舒适系数 `0.55`**：本产品是工间拉伸而非活动度评估，目标须落在舒适区内。颈椎活动度随年龄显著下降，若按青年人极限设定，年长用户即使动作正确也会被判为未达标。
    3.  **宽容带**：`±8°`，即实际满分触发点为目标角再宽松 `8°`（如侧倾 `17°` 即可满分）。超出目标不扣分，只惩罚未到位。
    4.  **常模出处与取值依据**：各文献给出的区间差异较大，上表取值及理由如下。

        **统一采用 AAOS 单一来源**，不再逐轴从不同研究里挑选数值。文献区间很宽（仅后伸就跨 `45°–80°`），混用来源会在无人察觉的情况下改变各动作之间的相对难度——而消除这种不均正是本推导存在的意义。

        | 轴 | 文献区间 | AAOS | 本表取值 |
        | --- | --- | --- | --- |
        | 前屈 | `45°–80°`（Kendall `65°`） | `45°` | `45°` |
        | 后伸 | `45°–80°`（Kendall `50°`） | `45°` | `45°` |
        | 侧屈 | `20°–45°` | `45°` | `45°` |
        | 旋转 | `80°–90°` | `80°` | `80°` |

        来源：[Cervical Spine Range of Motion (Orthofixar)](https://orthofixar.com/special-test/cervical-spine-range-of-motion/)、[Cervical Spine Ranges of Motion (Learn Muscles)](https://learnmuscles.com/glossary/cervical-spine-ranges-of-motion-rom/)、[Normative Values for Cervical and Lumbar ROM in Healthy Young Adults (J Turkish Spinal Surgery, 2023)](https://jtss.org/articles/doi/jtss.galenos.2023.33042)。

        ⚠️ **侧屈的风险**：文献区间下沿仅 `20°`，而本表按 `45°` 推导出目标 `±25°`。对活动度处于区间下沿的用户，该目标超出其极限。所幸 `±8°` 宽容带把实际满分触发点拉到 `17°`，仍在其可达范围内——但**若后续调整宽容带，须重新核算此项**。

    5.  **文献一致确认**：上述常模均为健康青年值，随年龄显著下降。这是舒适系数取 `0.55` 而非更高值的直接依据。

    6.  **实测校验（单人三轮，2026-07-23）**：

        | 动作 | 三轮 P90 | 波动 | 满分线占最好成绩 |
        | --- | --- | --- | --- |
        | 左侧倾 | `46.8 / 36.6 / 39.0` | `10.2°` | 32% |
        | 右侧倾 | `53.3 / 49.3 / 36.7` | `16.6°` | 32% |
        | 左转头 | `72.7 / 76.5 / 66.7` | `9.8°` | 46% |
        | 右转头 | `72.5 / 78.5 / 75.1` | `6.0°` | 46% |
        | 低头 | `42.9 / 44.1 / 34.6` | `9.5°` | 39% |
        | 仰头 | `31.5 / 25.3 / 33.5` | `8.2°` | 51% |

        结论三条：

        *   **「做到最大」本身有 `6°–17°` 的天然波动**，单轮 P90 不是稳定量，不能作为调参依据。
        *   **后伸是唯一离群轴**。该用户各轴最大值对常模之比为：侧屈 118%、旋转 98%、前屈 88%、**后伸 56%**。修正前后伸满分线占其能力 75%，其余动作仅 32%–46%，难度严重失衡。改用 AAOS 统一来源后收敛至 32%–51%。
        *   **追踪率 100% 只能排除丢帧，不能证明角度准确**。仰头时人脸透视压缩，Vision 完全可能在检测成功的前提下压缩 `pitch` 读数。故「摄像头低估后伸」与「该用户后伸受限」无法区分——**也无需区分**：评分只能对其观测得到的量打分，标定须在测量空间内完成。

        ⚠️ **单人数据的用途边界**：以上数据用于**暴露轴间不均**，不用于反推阈值。本次修正是消除来源混用（逐轴挑数改为 AAOS 统一），而非拟合该用户的数值。

        ⚠️ **静态保持 ≠ 跟练**：校准测的是静态最大幅度保持，真实跟练是跟随节奏做重复动作，实际幅度通常更低。上表余量属乐观估计，须以完整跟练实测复核。

*   **关于「收颌」动作的移除**：
    原第 5 个动作名为「低头收颌」，混淆了两个不同动作。**收颌（chin tuck）是下巴水平后缩的平移动作**，头部俯仰角几乎不变，摄像头基于 `pitch` 根本无法测量——这正是它此前目标角被迫设为 `18°`（远低于其他动作）的原因。现已明确为**低头（前屈）大动作**，可由 `pitch` 正常测量。

*   **公开保健操标准的适用边界**：
    米字操、工间操等公开标准规定的是**动作选择、节奏、保持时长与组数**，其动作终点是主观判据（"至有牵拉感"），**不提供角度度数**。故可直接引用其动作编排与节奏，角度阈值仍须按上述常模推导。

*   **对齐环收紧逻辑**：计算影子关节与目标关键点的空间距离。到位比例从 0 渐变至 1.0。当大于 `0.9` 且保持不变时，触发靶心圆环向圆心合并，开启 3 秒的 `HoldingTimer`。3 秒倒计时结束，调用 `LocalDBManager` 累计当前动作得分，并通知 UI 触发 `current_action_id` 递增进行动作切换。

### 7.5 角色资产与统一动画图 (PetAnimationGraph)

*   **生产资产**：人形小草 Rig v2 的网格、32 关节导出骨架、蒙皮、动作 manifest、坐标和认证门禁统一由 `pet-character-rig-v2.md` 定义。当前 24 骨资产只作为迁移期正式回退和视觉参考。licensed basemesh 必须先通过独立 source intake，再以 `licensed_basemesh_adapted` 新版本进入完整 S0；源文件既有拓扑质量、modifier、rig 或动作不能继承审批。
*   **静态拓扑合同**：App 的 `sprout-rig-v2-static-topology-contract-v2.json` 是 profile、关节环、support route 和严格几何阈值的机器真源。`scoped_wip` 永远不能产生 S0 pass；只有绑定合同版本/哈希且语义声明完整的 `open_five_s0` formal candidate 可进入静态审核。关节环点数可按局部形态变化，但同一关节相邻环必须等点数并构成连续 quad strip，左右结构须满足合同差值；生产 32 关节骨架不等于每个变形环固定 32 点。
*   **多宠物复用边界**：后续宠物复用的是骨架语义、动作 ID、认证流程、导出格式和运行时动画图，而不是强制复用同一身体网格、脸型或蒙皮权重。比例和拓扑兼容时可重定向同一动作源；体型、肢体粗细或脸部结构差异超过合同容差时，必须为该宠物单独重拓扑、蒙皮和通过变形门禁，避免套模造成塌陷或不自然表情。
*   **动作来源**：挥手、张望、伸展、庆祝等表现动作在 Blender control rig 中制作并逐帧烘焙到固定导出骨架。Swift 不再定义这些动作的具体关节角度；现有 `PetIdleMotionPlanner` 迁移后只保留随机动作调度职责并更名为 `PetIdleActionScheduler`。
*   **唯一姿势写入者**：`PetAnimationGraph` 按 Base → Action → Additive → IK → Secondary → Corrective 顺序合成完整姿势，一次性写入 `jointTransforms`。`PetSceneView` 只负责场景、相机、灯光、生命周期和最终提交。
*   **可保留能力**：六个头颈教学动作语义、`PlantedFootSolver`、叶片受限弹簧和 Rig probe 保留；教学动作的具体姿势逐步迁移为采样 clip。
*   **主发布路径与 Blend shape 边界**：最小往返实验已选择 Blender 4.3.2 → 原生 USD → USDZ → RealityKit。该路径保留骨名、31 个关节/shape 采样和 `TestBulge 0 → 1 → 0`；GLB 经 Apple 导入会匿名化骨路径、丢失 shape，且 RealityKit 不驱动测试骨，故只作交换与诊断产物。App 最低支持 macOS 14，而直接 shape API 要求 macOS 15，因此 corrective 与面部 shape 只能作为 macOS 15+ 可选增强，不能成为身体完整性的必需条件。

### 7.6 桌面宠物透明动作资源与视线随动 (PetSpriteRenderer)

*   **形象与动作单一来源**：大厅中央宠物、桌面悬浮宠物和跟练教练均由当前宠物选择驱动。3D clip 与桌面 strip 使用相同角色版本、DCC action、相机、灯光和发布 manifest，不混用旧形象或其他 rig。
*   **独立动作资源**：每个动作发布一条透明横向 sprite strip。manifest 定义 `clipId`、`frameCount`、`fps`、`loopMode`、逻辑画布、anchor、事件和 Reduce Motion 替代项；新增动作只增加自己的资源，不修改无关动作。
*   **迁移兼容**：Rig v2 切换前继续读取当前 `pet_sprout_sheet.png`。新资源加载或校验失败时回退当前正式图集或 `idle-neutral`，不得显示空白宠物。
*   **随机调度**：静默状态每 `3–8 分钟` 从呼吸叶摆、张望/挥手、伸展轻弹中选择一项，并排除上次动作；疲劳、提醒、点击和跟练等高优先级状态可立即打断，动作图根据 manifest 的可中断窗口平滑退出。
*   **16 方向眼珠/视线随动数学公式 (Look-Vector Mathematics)**：
    1. 获取鼠标在屏幕的全局绝对坐标 $M(x, y)$。
    2. 获取桌面宠物窗口中心点的绝对坐标 $P(x, y)$。
    3. 计算从宠物指向光标的角度向量：
       $$\theta = \operatorname{atan2}(M.y - P.y, M.x - P.x)$$
    4. 将弧度角转换为顺时针度数，并将朝上定义为 $0^\circ$。
    5. 通过除以 $22.5^\circ$ 进行四舍五入并取模，计算出 0 到 15 的盯人帧偏移量 `lookIndex`：
       $$\text{lookIndex} = \left( \operatorname{round}\left( \frac{\theta_{\text{degrees}}}{22.5} \right) \right) \bmod 16$$
    6. **帧映射规则**：`lookIndex` 直接选择 `look-direction` 独立资源中的第 `0...15` 帧；光标静止或离开敏感区时平滑回退 `idle-neutral`。运行时不依赖固定 Row。

*   **NSPanel 浮动窗口穿透与避让配置**：
    创建类 `PetNSPanel` 继承自 `NSPanel`，配置如下物理参数：
    ```swift
    class PetNSPanel: NSPanel {
        init() {
            super.init(
                contentRect: NSRect(x: 0, y: 0, width: 196, height: 176),
                styleMask: [.borderless, .nonactivatingPanel, .hudWindow],
                backing: .buffered,
                defer: false
            )
            self.isOpaque = false
            self.backgroundColor = .clear // 保证边缘绝对透明
            self.hasShadow = false // 禁用默认系统直角阴影，改用WebP自身半透明影
            self.level = .floating // 置顶悬浮
            self.collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary] // 跨屏幕展示
            self.isMovableByWindowBackground = false // 禁用系统默认拖动，改用我们的物理鼠标Drag
        }
    }
    ```

### 7.7 养成持久化与内购 (SwiftData & StoreKit 2)

*   **持久化**：  
    每次运动结算后，调用 `ModelContext` 向本地 SwiftData 写入一条新的 `WorkoutSession` 和 `HealthScore` 数据。
*   **StoreKit 2 会员控制**：
    在 App 启动时，开启异步任务查询内购特权：
    ```swift
    func checkSubscriptionStatus() async {
        for await result in Transaction.currentEntitlements {
            if case .verified(let transaction) = result {
                if transaction.productID == "com.nickbody.app.pass" {
                    // Unlock premium ranks and exclusive pet animation rows
                    DispatchQueue.main.async {
                        self.isPremiumUnlocked = true
                    }
                }
            }
        }
    }
    ```

---

## 8. 实现顺序与测试基准

### 实现顺序
1. **已完成的产品链路**：`FatigueTracker`、Vision 姿态识别、六动作评分、透明 `NSPanel`、大厅/跟练 RealityKit 场景和迁移期桌面图集保持可运行，不因资产重构中断。
2. **Rig v2 合同**：冻结视觉身份，完成 32 关节骨架、网格/蒙皮、动作 manifest、导出和 Gate 0–4 认证规范。
3. **最小往返实验（已完成）**：隔离三骨资产已验证 GLB/USD/USDZ/RealityKit，选择 Blender USD → USDZ 主发布路径；机器报告位于 App 仓库 `character_pipeline/sprout/v2/reports/roundtrip-report.json`。
4. **角色生产**：CHANGE-082 [边界来源与接续核查](./pet-character-core-constraint-design.md)完成剩余 96 段、96 个固定连接点，复用前轮切缝，边界来源阻塞解除。下一项核查既定 CGAL 原生入口与一份自由核心输入，尚无新底网或模型质量改善。CHANGE-074 实物仍拒绝，模板布点、CHANGE-076 接入、B1、权重及旧路线关闭。完整身体、S0、身份/UV、32 骨及动画未完成，正式资源与原合同保持。最后核实余额 190，本轮未查询或消费。
5. **运行时迁移**：新增 `PetAnimationLibrary`、`PetAnimationGraph` 和 `PetIdleActionScheduler`，迁移脚底/叶片后处理，保留旧资产回退。
6. **动作内容**：依次制作挥手、张望、伸展，每项执行八方位逐帧审核并生成同源桌面 strip。
7. **正式切换**：用户批准后更新不可变发布 manifest 和 bundle 资源；验证失败时回退当前正式资产。

### 技术风险防范
*   *风险*：Vision 姿态推理在 M1 基线机型发热高。  
*   *防范*：跟练开始时，限制摄像头捕获分辨率为 `640x480` (姿态识别不需要 4K 高清)；在 Vision 识别结果中，一旦检测到用户肩膀离场（`OutOfFrame`），立刻挂起推理，提示用户回到画面。
*   *风险*：Blender、GLB/USD、Apple USDZ 与 RealityKit 对骨架、单位、法线或 blend shape 的解释不一致。
*   *防范*：正式角色统一从 Blender 原生 USD 打包 USDZ；每次导出记录版本与语义合同哈希，使用 `usdchecker`、结构脚本和 RealityKit 原尺寸截图共同验收，禁止将 GLB 往返结果或发布文件覆盖 DCC master。
*   *风险*：自动蒙皮在肩、腋下、髋部产生远端权重污染。
*   *防范*：生产主身体重拓扑后手工蒙皮，使用命名区域 mask 阻止跨区域 influence，并按关键单关节 → 全关节 → 组合姿势顺序逐级放行。
*   *风险*：外部 basemesh 许可、来源文件或归因在适配与发布之间漂移，或来源身份被误当作拓扑通过。
*   *防范*：source intake 绑定 artifact、source topology、license evidence、attribution 和 manifest hashes；候选比较禁止 provenance 原地变更，发布前重新校验归因与修改说明。许可通过与 S0 几何/身份门禁分别判定。
