# Sprout 核心四边面联合约束设计

> CHANGE-091 最新结果见 §20 及[八类问题的方法与验证清单](./pet-character-method-validation.md)：86 个核心点排除当前不变三角底网的仅配对路线；14 面也不能靠翻转解决。四点提案保留、暂不优先施工，原件恢复不等于模型通过。§1–19 保留历史记录，生产合同及 §14.5 主动复盘规约保持。

## 1. 本轮结论与适用范围

采用一份有限选择设计：保留由固定环点确定的导向环带，从原参考源重新建立其余区域的候选单元，同时选择面连接、保护区实际邻接及合格几何。固定坐标不作为优化变量。这个设计补上普通三角面配对缺少的四价约束，但尚未证明真实输入上有解。

当前只准入**输入准备的单次验证**，不直接生成四边补片。剩余缺口是与原接口相容的三角底网、跨接缝身份、实际候选四边面及支撑条带集合。后续求解失败只说明登记的有限集合或其保守筛选未找到解，不能推断角色整体无解。

不调用已关闭的模板/调和布点构造器，不将 CHANGE-072/074 的失败核心冻结复用。原图与源三角对应可只读使用；任何将来新建的辅助展开图只用于源定位，不成为四边顶点的调和布点方案。

## 2. 已核实的输入事实

| 输入 | 本轮只读事实 | 对构造的影响 |
|---|---|---|
| 原裁剪源 | 4,705 个未切缝顶点、8,865 个三角；切缝后 4,738 点 | 原三角数为奇数，无法原样两两配完 |
| 原源两端边界 | 349 / 196 点 | 不等于需要保持的 64 / 32 点；必须新建相容连接 |
| 原固定点 | 256 个 float32 点逐位保持；其中只有 2 个与原源顶点转 float32 后精确重合 | 需用原源绑定插入身份，不得吸附到最近源顶点 |
| 固定环与旧接缝 | 四条导向环及精确缓冲环在存储周期参数中均有一次跨缝，且无点正好位于缝端 | 不能在旧二维图中直接连首尾或自动插点拆环；此结果不代替真实直线相交认证 |
| 固定导向环带 | `.16/.17/.18/.19/.195/.20` 六环、192 固定点、五条带共 160 个存量面 | 可登记为强制单元候选，不等于已取得完整合格核心 |

160 个面按原声明的固定环间对应关系确定，并在 CHANGE-072 存量连接中逐面找到。原独立报告的双对角线折面、边长比超限及完整自交列表均未涉及这些面。原补片→源最大值 1.572449575 mm 的采样集合包含这些面的采样，因而提供它们到原完整参考源的最大值上界；没有单独测量环带 P95、反向贴合或完整核心门禁，不补写通过结果。

输入来源：App `6004e40` 绑定的原 `chart-cut-disk/chart.npz`（SHA-256 `d12761f15843b69cc4a00db6d19f1461f89fb58701ac1f6efe5bb3fac56f8dea`）、原 `placement/final/patch.npz`（`23c52ca42f01ec5539333bff611d03f206793cd739bd909faf474b55ee42923c`）、CHANGE-072 的固定身份/声明/独立报告。只取固定点、源绑定和上述环带，不取旧失败核心的点位或面连接。

## 3. 输入准备合同

输出一份混合输入 M＝固定环带 F＋可配对三角域 T。F 为上述 160 个面；自由区域覆盖原近端 64 点边界到 `.16` 环之间的核心及过渡域。这里的“自由”仅指连接可重建，原 256 点仍不可移动。

| 数据 | 来源与要求 | 拒绝条件 |
|---|---|---|
| 固定点表 | 原点 ID、float32 字节、原源三角/重心绑定；两端口、四环、`.195` 缓冲 | 字节改变、身份合并或源绑定误差超过原审计 0.5 mm |
| 内部点集合 | 原参考源在允许域内的顶点；辅助切缝确有需要时由原源边/三角交点取得，逐点记录重心来源 | 采用旧失败核心坐标；无源归属、域外点、非有限数值 |
| 固定边和面 | 原环及接口逐段保持；F 的有向连接单独保存，不送入自由域重网格 | 多插一段、少一段、重复或反向面、固定面被细分 |
| 切缝身份 | 新切缝需经过两端口与各固定环的既有顶点；在环带内沿已声明纵向连接 | 穿过固定边内部后增点；以坐标近似焊接代替明确身份 |
| 源域边界 | 原源中证明切缝简单、环次序一致、边界无交叉；切缝两侧建立明确商空间映射 | 仅凭二维外观认定相容；归属、覆盖或焊接后拓扑不成立 |
| 三角连接 | 仅对登记自由域做一次受约束三角化；内部点集预先冻结，不运行细化/平滑 | 三角方向失败、重复、非流形、错误边界或奇数 T |

新切缝尚未构造或认证。现有原源与固定环数据提供锚点来源，但不保证所需简单切缝、曲面展开及直线约束同时成立。下一次输入预检必须先完成这项；失败便停止，不靠拆固定边处理。

候选底网的平面点必须携带独立身份。CGAL 的顶点 `info` 可存身份，但相同二维位置可能只形成一个顶点，因此导入后仍要核对身份映射为一一对应；合法切缝别名由单独记录管理。最终边数、顶点价数与一圈全部在**合并明确切缝别名后的实际曲面**上计算，不在切开的二维图上计算。

域内外标记必须区分真正外边界和内部导向约束。CGAL 6.2.1 的 `mark_domain_in_triangulation.h` 跨每条约束边都增加嵌套层数，再按奇偶判内外；直接把内部环送入该默认标记会产生错误的洞。设计要求按已认证外边界标记，跨内部导向边保持同一域，不使用这个默认奇偶结果作为域认证。[固定发行版源码](https://github.com/CGAL/cgal/tree/v6.2.1/Triangulation_2/include/CGAL)。

## 4. 有限候选与联合约束

以下是本项目推导的离散设计，尚未编成或运行求解器。前提是 M 已为相容、连通的实际曲面网格，T 为有限三角集合，F 的单面检查不失败。

### 4.1 面覆盖和保留边

每两个共享一条可删除边的 T 中三角最多形成一个候选四边面 q。四顶点必须不同，边界简单、有向一致；双对角线折面、退化、边长比 >3.5 的候选直接排除。原接口、固定环、F 边界及其他登记保留边不参与删除。单面筛选使用最终 float32 点位。

设 `x_q ∈ {0,1}` 表示选中候选，`y_e ∈ {0,1}` 表示实际边 e 保留：

- 对每个三角 t：`Σ(q 含 t) x_q = 1`，保证每片恰好覆盖一次。
- 对可删除边 e：`y_e = 1 − Σ(q 删除 e) x_q`；其他边 `y_e = 1`。
- 固定面 F 不参与配对；保留它们的坐标和连接。

这保证的是登记底网上的完整配对。无法覆盖的三角、无候选的区域、保留边冲突均应给出编号；不得增加虚拟邻接、复制固定点或输出夹杂三角面的结果。

### 4.2 原核心与最终真实一圈

所有候选顶点位置固定后，核心 C 按原定义确定：75 mm 球内顶点加四条原导向环。设实际度数 `d_v = Σ(e 接 v) y_e`。用二进制 `p_v` 标记最终保护集合：

- `v ∈ C` 时 `p_v = 1`。
- 其余 v，对每条通向 C 的候选边 e，约束 `p_v ≥ y_e`，且 `p_v ≤ Σ(e 从 v 通向 C) y_e`；无此边则 `p_v = 0`。
- 用已知初始度数选择有限常数 `M_v = degree_M(v) + 4`，施加 `−M_v(1−p_v) ≤ d_v−4 ≤ M_v(1−p_v)`。

因此保留下来的邻边才决定一圈成员，成员度数必须为四。不能先从某个旧网格固定 P，也不能把切缝两侧的重复邻居计算两次。对 F 与新域交界也执行同一规则，避免新接入核心边后产生极点。

### 4.3 相交和支撑条带

单面合格但共同选中会发生非法相交的候选对 `(q,r)`，加入 `x_q + x_r ≤ 1`；与 F 相交的候选直接排除。共同顶点/边的合法接触按原审计口径处理。宽阶段筛选可以加速，不能因预算不够省略冲突对；冲突表不完整时拒绝求解准入。

F 已提供固定环之间的实际四边条带。肩顶与腋下仍需从**新候选连接**形成成对、不共享顶点且围成连续四边条带的候选 s，带上原语义几何检查及不复用关节环的结果。对每类条带选一个 `z_s`，并要求 `z_s ≤ x_q`（条带使用的所有 q）。被选条带上的点及最终实际邻居也施加四价条件，可用 `z_s`、`y_e` 的联合激活表达。连接方向仍须 ≥0.85。

条带候选的实际枚举尚未完成；没有对应集合，就不能把只有覆盖/四价的解称为完整肩腋通过。旧失败核心的支撑路径 ID 不直接沿用。四个环仍仅是拟用上臂导向，未获肩关节位置认证。

### 4.4 双向贴合的可编码条件

源→补片仍使用原 35,460 个源三角中心和边中点。对每个源采样 i，预计算哪些候选/固定面距它 ≤2 mm、哪些 ≤1 mm；前者要求至少选中一个，后者用二进制 `h_i` 计覆盖。为避免不同四边剖分造成遗漏，预计算采用两种对角线距离的较大值作为保守上界；最终仍执行原审计。

补片→源保持原协议：所有顶点、实际唯一边的中点、每面中心及 4×4 双线性网格。顶点计数固定，边样本由 `y_e` 激活，面中心及 16 内部样本由 `x_q` 激活；F 的相应样本作为常量。对距离 >2 mm 的必选样本立即拒绝，对可选样本禁用其边/面。双线性内部点必须实际测量，不能以原两片三角的误差替代。

在不增删顶点、一次配对只删一条内部边的前提下，令 `V,E_M,F,T` 表示实际混合网格计数，最终采样总量固定为 `N = V + E_M + 17F + 8T`。这来自原协议 `N=V+E+17Q`、`E=E_M−T/2`、`Q=F+T/2`；后一个原协议公式与两份存量报告的 24,416 / 14,416 采样数核对一致。

为表达 P95，使用一个**充分但稍保守**的计数条件：超过 1 mm 的激活样本数 ≤`floor(0.05×(N−1))`。源方向即至少 33,688/35,460 个采样满足 1 mm，全部满足 2 mm。这个条件能保证默认线性插值 P95 ≤1 mm，但不等价于原阈值的所有边界情况；不得把它导致的不可行解释为原合同不可行。[NumPy 2.4 的百分位定义](https://numpy.org/doc/2.4/reference/generated/numpy.percentile.html)。

该设计没有放宽原 1/2 mm 门槛，也没有把有限采样升级为整个连续曲面的误差证明。预计算须绑定原源、float32 输入和原审计距离口径；数值边界或不同剖分的差异必须由最终独立检查确认，不能用求解器容差豁免几何失败。

## 5. 实现接口核查及结果解释

CGAL 固定到 `v6.2.1` / `28811b671a12b5caa9e3688569dadbc6b3728fe6`。已下载官方 library 发行包，SHA-256 `8e7ce1e30e8ffcb3e69d153c3a561bb1f38bbb4becd8e6544cbaa0fb2889af62` 与官方发布资产摘要一致，检查约束插入、顶点身份和域标记头文件。约束插入仍会沿已有点拆为子段，严格交叉标签也不代替原始分段核验。[官方发行版](https://github.com/CGAL/cgal/releases/tag/v6.2.1)。相关三角化头文件为 GPL-3.0-or-later 或商业许可；本轮只读源码，未安装、编译、链接或分发，不加入 App 依赖。

SciPy `1.17.1` 的 `_milp.py` 与已有隔离环境对应文件逐字节一致，SHA-256 `a840bc816dce9e9ca995e2c4cd8d85328c5bbe9656555e74951f798d1eb90482`。接口支持稀疏线性约束、显式整数变量和上下界；二进制变量应设置整数类型及 `[0,1]` 边界。已有 Python 封装一致不等于原生求解器已运行或整个流程可用，本轮没有调用 HiGHS。[固定版本源码](https://github.com/scipy/scipy/blob/v1.17.1/scipy/optimize/_milp.py)。

首轮只做可行性，目标系数为零，不同时开展美观权重搜索。未来执行时记录时间/节点限制、原始 status、x、message、约束矩阵和输入指纹。原始二进制解须通过独立整数约束复算和全部几何/语义审计后才能导出。

| 求解结果 | 允许解释 |
|---|---|
| 有完整解且原合同独立审计通过 | 该有限输入存在一份 scoped_wip 合格解，仍不是 S0 或骨架入口 |
| 求解器报告不可行 | 该有限模型被报告不可行；保守距离/P95条件和候选点集均限制了范围，不是形式化的角色无解证明 |
| 超时且没有可核验解 | 未知；不得写“不可行” |
| 超时但有候选解 | 保留原状态，独立核验后才可报告有限可行性，不宣称最优 |
| 解通过整数条件但几何/语义失败 | 拒绝，不导出模型；记录是哪一项输入、编码或数值检查漏掉了条件 |

## 6. CHANGE-078 登记的单次输入验证（已由 §8 执行结项）

下一项在新登记的 **60 分钟总预算、前 30 分钟输入预检截止**内，最多准备一份切缝/约束方案及一份相容三角底网。运行入口、切缝和预检均计入前 30 分钟；先核对原锚点和简单切缝、固定环带边界、内部导向标记及源归属，前置失败即不调用三角化。通过后最多一次 CGAL 三角化，无细化、换点集或参数重跑。

交付应包含：原 256 点和 F 的字节/连接核对、明确切缝别名、自由域与固定环带归属、三角/边计数及奇偶、焊接后的流形和边界检查、源绑定、真实核心和一圈的可用度数范围。先用这些结果判断联合模型是否具备输入，**本次后续项不调用 MILP、不生成四边角色、不启动 Blender 或付费操作**。底网只是新的受约束连接输入，不能冒充模型质量改善。

若输入具备，才登记一轮候选面/条带/冲突/距离预计算与有限求解；其规模和预算须据实际输入确定。未具备时记录具体接口失败，停止该输入方案，不返回旧核心重审，也不转工具轮换。

## 7. 本轮证据与执行

UTC 09:26:04 起，09:56:04 设计截止、10:26:04 总截止。只读源/固定身份核对、160 面存量归属核对、固定发行版源码及原采样计数核查。零新网格、求解、工具安装、Blender、付费或产品代码改动；没有新产品测试通过数。

私有证据目录 `sprout-core-joint-constraint-design-20260920/`：`source-input-review.json`、`fixed-guide-band-review.json`、`implementation-source-binding.json`、`sample-count-check.json`、`source-downloads.json`；原始源/合同及此前证据共 441 项冻结，App `6004e40`、用户根工作树和原 4,725 个未跟踪路径保持。最终设计收敛、保全、面板推送与工时见 `receipt.json`。

## 8. CHANGE-079：全环带输入在三角化前被拒绝

### 8.1 实际方案与通过的局部检查

本轮只准备一份输入：沿原固定 ID `0/832/864/896/928/960/992` 定义单调纵向切缝 `u=s(t)`，以 `r=(u−s(t)) mod 1` 导入固定环；每个锚点保留明确的左右别名，原三维坐标不变。源绑定仍经过原连续参数映射及原三角重心定位，没有把旧周期参数顶点直接线性连接为参考源，也没有调用旧四边模板或调和布点。

| 检查 | 本轮实测 | 允许结论 |
|---|---|---|
| 固定身份 | 256 个 float32 点逐位一致，160 个存量环带面逐一找到 | 固定输入可追溯；不是完整核心通过 |
| 固定点源绑定 | 最大误差 0.0000160651 mm | 小于原 0.5 mm 门槛 |
| 导入平面点与边 | 263 点（含 7 对切缝别名）、423 条边 | 同一份选定输入，没有生成三角底网 |
| 环顺序及相邻环间隔 | 七环周向均递增，六组相邻环间隔均为正 | 各环作为分段线性图互不交叉；不保证纵向连接也无交叉 |
| 环带面积 | 自由域 0.717442741279168、固定带 0.28255725872083237；160 面有向面积均正 | 面积和方向条件通过仍不足以认证完整边嵌入 |
| 旧接缝身份 | 33 组源别名，纵向参数最大差 8.88×10⁻¹⁶ | 已存顶点对应一致；未据此认证整段连续源切缝 |

辅助检查还发现：若将旧周期参数当作直线三角图，8,865 片中有 27 个负面积、2 个近零面积三角，29 片仅 1 片接触旧源切缝。因此拒绝这个线性化解释；原源和原已认证度量图仍有效，未被本次推翻。本轮实际输入使用连续源查询，这个辅助结果不是其 3 处交叉的判定算法。

### 8.2 导致停止的具体交叉

独立检查器对 423 条导入边使用存储二进制浮点数的精确有理数方向判定，检查非相邻相交、不同身份端点接触及共享端点后的重叠，得到以下三对非法交叉：

| 原固定边 | 相交的原固定边 | 所属区域 |
|---|---|---|
| `961–993` | `930–962` | `.195→.20` 连接与 `.19→.195` 连接 |
| `961–993` | `962–963` | `.195→.20` 连接与 `.195` 环边 |
| `961–993` | `962–994` | 两条 `.195→.20` 连接 |

三处都在固定环带末端，**不在 `.16` 以下自由核心域的边对中**。这些是所选二维展开输入的交叉，不是三维环带的新自交报告；不能由此撤销 CHANGE-078 的存量报告事实，或判断原固定点无法构造四边面。局部顺序/面积通过但完整连接失败，也说明不能把前一组 `passes=true` 当作整个输入通过。

发现后停止该输入方案，没有换锚点、移动点、拆固定边、重新切缝或调用三角化。源对齐的完整切缝、源域覆盖、底网流形/价数及后续联合解均未取得；未测项没有补写通过。

### 8.3 运行入口与本轮交付

CGAL 仍绑定官方 6.2.1 发行包。复用本机 GCC 13.2.0、Boost 1.84.0、GMP 6.3.0、MPFR 4.2.1，未安装编译器或依赖包。三次编译入口准备分别遇到失效的默认 SDK 路径、桥接程序版本宏错误和 Xcode 许可拒绝；前两项做了同一入口内的配置/代码修正，最终未取得可运行程序。没有替用户接受许可，没有执行 CGAL 三角化或改换工具。桥接源码只作未执行档案，不作为已验证工具发布。

App 新增固定输入预检与独立平面边检查器，16 项合成测试通过；这只证明这些 Python 检查的行为，不证明 CGAL、真实源切缝或角色静态门通过。原始输入没有重建，零新底网/四边角色/MILP/形状优化/Blender/付费，模型质量没有改善。

UTC 09:56:29 开始，10:14:07 的完整边检查触发停止，10:23:31 登记预检结论，早于 10:26:29 预检截止；10:56:29 为包含归档、检查、提交和推送的总截止。原 463 项冻结文件、用户根工作树和 App 原 4,725 个未跟踪路径保持。App 证据位于 `character_pipeline/sprout/v2/work/experiments/sprout-core-input-preflight-20260920/`；私有同名目录保存保全、编译错误摘要、发行包提取清单及最终 `receipt.json`。仅移除本轮新增的可重建头文件展开目录，原官方压缩包和历史证据保持。

### 8.4 后续范围：先拆清二维域与固定三维环带的职责

下一项由 @winston（AI 执行）做一次 **30 分钟设计、60 分钟总预算**的域分离核查，零新网格/编译/求解：自由核心只到 `.16` 公共接口，160 面固定带继续以原三维连接保留。原 64 点近端口与 32 点公共环仍须逐点、逐边保持。需要明确公共环身份、仅覆盖自由核心的源域与切缝，以及后续审计如何仍覆盖整块三维网格。固定带不进入自由域三角化是原设计 §3 的既有边界；本轮额外要求整个固定带共用一个直线平面图尚未成功，不能反过来把它当作合同要求。

该职责分离尚未完成，不是另一个已通过输入，也不自动授权再次三角化。若源域/公共接口仍无法落实，记录具体缺口并停止；不得通过拆边或省略固定带三维几何/真实一圈审计绕过合同。任何将来的 CGAL 实际调用还须先解决本机许可导致的原生入口阻塞；即使入口解决，也不能单独解除几何门槛。

## 9. CHANGE-080：域分离设计与公共接口核查

### 9.1 结论与外部依据

固定带 F 不需要进入自由核心的二维三角化输入。保留其原三维坐标及 160 面连接，未来只沿 `.16` 公共环装配新增域 T，是一份有依据的设计。复用 CHANGE-079 存储坐标后，自由域边界没有原全环带的三处交叉；这是输入条件的推进，没有新模型或质量改善。**新切缝的逐三角来源仍未落实，三角化准入保持未通过。**

本次定向阅读原作者文档，未安装或调用其中的新工具：

| 原始资料 | 可借鉴部分 | 对本项目的限制 |
|---|---|---|
| [CGAL 参数化手册](https://doc.cgal.org/latest/Surface_mesh_parameterization/index.html) | 先定义适当拓扑的域及切缝；切缝两侧保留独立参数身份 | 参数化保证有前提；不将原源顶点周期坐标直接连成已被拒绝的线性三角图 |
| [CGAL Seam_mesh](https://doc.cgal.org/latest/BGL/classCGAL_1_1Seam__mesh.html) | 用虚拟边界表达切开的曲面 | 接缝须是底层网格已有的内部边，不能直接把任意参数曲线当作已支持输入 |
| [CGAL 显式边对拼接](https://doc.cgal.org/latest/PMP_Mesh_repair/group__PMP__combinatorial__repair__grp.html) | 可明确指定拼接边对；拓扑检查区分相反有向边和流形性 | 拼接不认证几何或四价；本项目优先共享原 ID，不按坐标近似寻找焊点 |
| [Geometry Central SurfacePoint](https://geometry-central.net/surface/utilities/surface_point/) | 用顶点、边上参数、三角重心位置记录曲面来源 | 点来源不是整条边的覆盖证据；保持既有 SourceChart 来源接口，不引入新库 |
| [SideFX Quad Remesh](https://www.sidefx.com/docs/houdini/nodes/sop/quadremesh.html) | 接缝连续性会限制面流；简化拼接可能牺牲特征约束 | 不据工具能输出网格推断固定坐标、分段或零极点全部满足，不启动 Houdini |

CGAL 快照页面标注 6.2.1；运行版本仍受 CHANGE-079 的原生入口阻塞，本轮没有编译重试。上述是方法依据，不是新增实现验证。

### 9.2 存量输入的只读实测

没有重算展开或更换锚点，只取 CHANGE-079 的近端、`.16` 两行以及原切缝函数 `s(t)`。未复制旧失败核心的连接。

| 检查对象 | 实测结果 | 结论范围 |
|---|---|---|
| 核心平面边界 | 98 点、98 边，含两个端点的切缝别名；精确有理数方向判定无交叉；有向面积 0.717442741279168 | 这一个平面边界合法；不等于三维底网合格 |
| `.16` 环相对近端边界 | 最小参数间隔 0.6211948182371906，最大环高度 0.8136363985059683 | 两条分段线性边界分离；`.16` 是原导向环名称，不是整环恒定的图参数值 |
| 固定带拓扑 | 192 点、352 边、160 面、Euler=0；一个面连通分量、两个各 32 边的边界；288 内部边方向相反，顶点邻域均为合法路径或环 | 原固定带具备环带拓扑；没有重跑或补写完整三维质量门 |
| 公共环 | 原 ID 832–863；32 条边各邻接 F 中一个面；全部顶点在 F 中度数为 3 | 新域须逐段共享这 32 条边，每点恰好增加一个原 F 中没有的邻点 |
| 原源接缝身份 | 33 对点、32 对边；源三维端点逐位相同，UV 镜像残差最大 1.256×10⁻¹⁵ | 原有离散接缝对应成立，不是新切缝的追踪记录 |
| 原源接缝双侧查询 | 每个旧接缝段查 1/4、1/2、3/4，共 96 对，最大三维差 6.271×10⁻¹¹ mm | 有限查询未见裂缝；独立查询圆盘两侧，未用会自动取模的同侧调用伪造一致 |
| 公共环曲线与固定三维边 | 每段同样查三个点，共 96 点；到各自固定直边的最大距离 0.955776821 mm、P95 0.521246674 mm | 仅曲线与对应弦的抽查；不是连续最大值或原补片双向贴合验收 |

原固定 256 点的 float32 字节与固定身份映射再次核对一致。上述距离不得替代 §4.4 的原 35,460 个源采样、最终四边面的完整采样或任一生产阈值。

### 9.3 自由域、身份与装配的明确约定

令原 `.16` 存储平面折线为 `t=g(r)`，自由域定义为 `D={0≤r≤1, 0≤t≤g(r)}`。边界两端均是原锚点 832 的两个参数别名；底边两端是原近端锚点 0 的别名。二维左右边在商空间中按相同 t 配对；公共环和近端口的原三维边不拆分。

源查询采用 `u=(r+s(t)) mod 1` 后进入原 SourceChart 的非线性参数到圆盘映射，再由原三角重心查询三维位置。`s(t)` 仍取 CHANGE-079 的全部原锚点，不重新拟合。新切缝仅是 `r=0/1, 0≤t≤0.7121720667825544`；这段内 `s(t)` 由原 0 与 832 两锚点唯一确定。剪切变换在周期参数域中可逆，但把它用于真实源域仍依赖原源两侧相容性及逐段来源核验；96 对抽查不升级成连续证明。

三种身份必须分开存储：原固定顶点 ID、二维切缝别名、源三角及重心坐标。同一原点可在二维出现两次，最终只能留下一个三维点。两侧接缝必须携带同一组逐段身份，不能各自采样再近似焊接。源定位辅助交点可以用作曲线证据，不能偷偷作为固定环的新增分段点。

未来 T 的 `.16` 边需沿与 F 相反的面边方向接入。F 中每个公共顶点已有两个环邻点和一个纵向邻点，因此 `degree(F∪T)=degree(F)+degree(T)−2=4` 要求 T 的该边界点度数为 3。不得只验证坐标重合而漏掉此条件，也不预先指定新增邻点是谁。装配后还需重新验证顶点邻域、单连通、原外边界及原核心/实际一圈。

计算切缝本身不新增产品固定边要求。合并别名后，切缝两侧三角的真实邻接要恢复；其公共边若不是合同保留边，应按 §4 的同样条件参与候选配对，避免因为二维切开而凭空限制三维面流。共同 `.16` 环始终保留，不可被配对删除。

F 不进入 D 的生成器，但 F 与 T 的共同自交、折面、支撑带及完整贴合必须一起审计。自由域定义也不能成为缩小原参考源采样范围的理由。固定带现有有限证据仍不等于完整核心通过。

### 9.4 剩余缺口与唯一下一项

缺口已经缩小到可执行的来源记录：新切缝和公共环逆映曲线分别穿过哪些原源三角，在原源边的何处进出；跨旧源缝时如何保留对应身份；这些记录是否无遗漏、无歧义地覆盖整个自由域边界。CGAL Seam_mesh 不能直接消费这条任意曲线，不能把 98 边无交叉当作该问题已解决。

下一项由 @winston（AI 执行）实现并运行**一次只读源域追踪与来源检查**，沿本节冻结的边界和切缝，30 分钟前置核查、60 分钟总预算。交付逐三角区间、交点重心来源、跨源缝身份及完整性结果；不移动原点、不拆固定环边，不生成三角底网或四边模型，不编译或运行求解器。遇到未能界定的曲线区间、来源歧义或固定条件变化立即停止，不更换切缝、密度或工具重复尝试。源域检查成立后，才另行处理既定 CGAL 原生入口和一份输入构造。

这一项是实际来源记录与检查器交付，不再只重复本节的接口设计。未取得该证据之前，完整三角化、候选集合、联合求解和模型改善均保持未完成。

### 9.5 执行与证据

UTC 2026-09-22 04:51:32 起，05:01:09 完成研究与只读核查收敛，早于 05:21:32 设计截止；05:51:32 为总截止。五份官方文档快照已绑定 URL 与哈希。App 保持 `5dd5b9f`，无 App 代码变更，零新网格/编译/求解/Blender/付费/依赖，不新增产品或检查器测试通过数。

私有 `sprout-core-domain-review-20260922/` 保存 `interface-review.json`、`fixed-band-topology-review.json`、`source-seam-review.json`、`interface-curve-review.json`、`source-intake.json` 和 `design-decision.json`。原 485 项保护文件、根工作树及 App 原 4,725 个未跟踪路径的最终保全、文档检查、推送与工时见 `receipt.json`。原合同和正式资源保持；没有整体无解或必须人工接手的结论。

## 10. CHANGE-081：新切缝来源已记录，完整边界追踪未通过

### 10.1 单次执行结果

在冻结的 CHANGE-079 坐标和 CHANGE-080 域定义上只运行一次，未更换切缝或增加固定分段。先查新切缝，再查 64 段近端口、32 段公共环，共登记 97 段。

| 对象 | 本轮实测 | 可作出的判断 |
|---|---|---|
| 新切缝 | 2 个仿射参数分段，1,622 个数值包围区间，65 个原源三角，64 次源边切换 | 这一条切缝的数值来源记录已完成 |
| 新切缝切换处 | 最大双侧三维差 3.093570171×10⁻⁷ mm | 在原 0.5 mm 绑定阈值内；不是整块贴合结果 |
| 近端原边 0–1 | 三角 6274→6273 的保守公共要素检查未能通过 | 整轮停止，不认证源域；不是源曲面存在裂缝的证明 |
| 后续边界 | 其余 63 段近端和 32 段公共环，共 95 段未访问 | 不得报告为失败或通过 |

没有新底网、四边模型或模型质量改善。新切缝的完成记录不覆盖未测边界、全域拓扑或所有连续浮点误差。

### 10.2 数值判定与证据范围

检查器对整段曲线构造向外舍入的数值范围，三角函数使用 Taylor 展开及余项界，保留仿射源边界上的相关性；不是用中点代替整个区间。分段后的仿射计算使用 64 个 binary64 epsilon 的 UV 补偿，三角归属沿用既有 SourceChart 的 `1e-7` 重心容差。该补偿及整个流水线没有形式化实数证明，报告明确保留 `source_domain_certified=false`。

三角切换检查另要求非公共顶点重心坐标的区间不超过 `4e-7`，这是检查器的保守判定条件，**不是新增的产品几何阈值**。它未通过只能表示当前检查未认证切换；不得推导原 0.5 mm 绑定条件或模型不可行。本轮没有调整这个条件后重跑。

实际运行前 14 项工具测试通过。停止后仅改进失败措辞和上下文保留，补充公共边、三维间隙和失败证据的 3 项控制，最终 17 项通过；没有第二次真实输入运行。档案 `executed-tools/` 保留实际运行的原字节，当前工具与原执行版本的差异单独记录，不能把新日志能力反填到原失败报告。

### 10.3 停止点的只读诊断

失败报告给出的两片源三角共享原边 `3354–3338`；其中 3354 是原近端边界顶点，另一个点在内部。原边界链中该顶点的相邻源点为 3358、3355。按既有源图在近端 `t=0` 的关系 `angle=pi/4+pi*u/2`，顶点 3354 的 `u=0.007992305491423877`，位于固定边 0–1 的参数 0.5115075514511281 处。

因此，原边界顶点事件是一个明确可核查的后续切入点。原失败版本没有保存该段的中间区间和实际失败参数；不能声称上述参数就是原程序停止的精确位置，也不能断言已证明全部数值原因。只读诊断没有重新追踪该段，没有移动点或改变容差。

### 10.4 唯一下一项及停止条件

下一项由 @winston（AI 执行）补充**原源边界顶点事件表示**：近端来源沿原 349 段有序边界及上述解析参数关系登记，在源图中划分来源区间，不把这些事件作为固定 64 段模型边的新顶点。先用合成的边界顶点/细长三角控制验证，再最多运行一次尚未完成的 96 段边界；已完成的新切缝证据只读复用。

总预算 60 分钟，前 30 分钟完成该表示和必要控制；不得通过调容差、改密度、更换切缝或重复调用本轮旧表示兜底。新表示仍无法给出完整区间和公共身份时停止，保留具体失败见证，不进入底网。此项不编译、不三角化、不求解、不生成模型，也不把原生入口的许可阻塞混同为几何结论。

### 10.5 交付与保全

UTC 05:09:24 起，05:23:04 单次追踪报告拒绝，05:25:37 收敛只读诊断，早于 05:39:24 前置截止；06:09:24 总截止。App 新增区间工具、追踪器、17 项测试及有界档案 `character_pipeline/sprout/v2/work/experiments/sprout-core-source-trace-20260922/`。零网格/编译/求解/新依赖/Blender/付费，不新增产品验收通过数。

私有同名目录保留执行计划、真实 stdout/stderr、原执行脚本、`trace/trace.json`、`boundary-event-diagnostic.json` 和 `preflight-decision.json`。原 500 项保护文件、原 256 点/固定带/核心与实际一圈、合同、正式资源及用户根工作树保持；原 App 4,725 个未跟踪路径保全。最终文档与档案检查、双仓提交/推送及工时见 `receipt.json`。当前仍无完整角色或必须人工接手的结论。

## 11. CHANGE-082：剩余边界来源与端点接续通过

### 11.1 本轮结果

冻结原 256 点、切缝和 CHANGE-080 域定义，只运行一次剩余 96 段；CHANGE-081 新切缝的 1,622 个区间、65 个源三角和 64 次跨边记录按 SHA-256 复用，没有重新追踪。近端原边 0–1 的来源阻塞解除。

| 检查对象 | 实测结果 | 范围 |
|---|---|---|
| 64 段近端固定边 | 全部通过；412 个来源区间覆盖原 349 条源边，348 次源顶点事件 | 来源事件没有插入模型顶点或拆固定边 |
| 近端映射与源边 | 最大三维偏差数值上界 5.038844951×10⁻⁹ mm | 对原边参数映射的检查，不是补片贴合 |
| 近端顶点事件 | 最大双侧差数值上界 1.010153195×10⁻⁸ mm | 明确原源顶点身份 |
| 32 段公共环 | 全部通过；4,313 个区间、186 个源三角、185 次跨边 | 最大切换差 9.321213718×10⁻⁸ mm |
| 分段接续和切缝锚点 | 96 个固定连接点、194 条端点记录通过 | 只读评估已登记端点载体，没有重复曲线搜索 |
| 固定点绑定 | 最大误差数值上界 0.000015178594 mm | 原 0.5 mm 阈值保持 |
| 端点双侧差 | 最大数值上界 5.369460157×10⁻⁹ mm | 包含两个切缝锚点 |

本轮 96/96 段通过，零失败、零未测；与已完成新切缝合并后，边界来源检查完成。`source_domain_certified=false` 仍保留，表示尚未认证整个自由域、相容底网或四边可行性，并非还有本轮边界未测。没有新模型或模型质量改善。

### 11.2 方法和数值边界

近端使用原有序边界与 `angle=pi/4+pi*u/2` 登记顶点事件。每个区间的来源是明确的原源边，两个端点权重表达沿边位置，三角的第三权重由边身份确定为零。现有参数曲线向该边投影，边参数做区间包围；法向数值偏差通过原相邻三角映射为三维偏差，并按原 0.5 mm 绑定阈值检查。

这解决的是边界载体表示问题，不靠细长三角的重心归属判定来选择源边。边参数保留既有 `1e-7` 数值归属容差，公共环仍使用 CHANGE-081 原三角与 `4e-7` 切换检查条件，均未调整后重跑。初版合成细长三角控制揭示沿用三角归属仍会放大误差，因此在真实运行前改为上述边表示；开发期合成失败如实保存在测试记录中。

区间运算、三角函数包围及 64 epsilon 仿射补偿沿用上一轮；投影系数和范数仍为 binary64 运算。结果是数值证据，不冒充整个实数映射的形式化证明。原生产几何、75 mm 核心/真实一圈、双向贴合和四价条件均不变。

### 11.3 验证、交付和保全

真实运行前 28 项工具测试通过（原来源工具 17 项、新边界工具 11 项）；随后新增 3 项端点身份/固定点漂移控制，当前共 31 项。实际追踪只有一次，05:55:23 完成；05:58:00 端点只读核查通过；05:59:59 收敛本轮，早于 06:17:18 前置截止。05:47:18 开始，06:47:18 为包含归档及推送的总截止。

App 档案为 `character_pipeline/sprout/v2/work/experiments/sprout-core-boundary-events-20260922/`；原始 trace 以确定性 gzip 无损保存，解压后 SHA-256 为 `7eebc3ec97934951c7c6fd9e3ed23b66ce1cd9a3a483c339279087138a683d10`。`executed-tools/` 保留真实追踪时版本；新增端点审计和三个测试发生在真实追踪后，没有改写或重跑 trace。私有同名目录的 `receipt.json` 记录最后保全、双仓推送和工时。

原 531 项保护文件、源数据、合同、正式资源和根工作树用户改动保持，App 原 4,725 个未跟踪路径保全。零底网/编译/求解/Blender/付费/新依赖，不增加产品验收通过数。

### 11.4 下一项：原生入口与一份相容底网输入

边界来源工作已完成，无新矛盾证据不再追踪或改表示。由 @winston（AI 执行）接续既定 CGAL 6.2.1 路线，30 分钟前置、60 分钟总预算：

1. 先读取 CHANGE-079 编译失败记录，核查本机已有编译器、Command Line Tools 和 SDK 的实际路径，明确原许可拒绝由哪一入口触发；不安装依赖、不代替用户接受许可。前置材料完整后最多一次桥接编译及必要合成固定身份/不可拆边控制，失败即保留明确入口原因。
2. 将 CHANGE-080 自由域边界、CHANGE-081 切缝和本轮来源表合为一份输入；原源域内点及必要切缝交点按一套规则登记并冻结，源边界事件只留在来源表，不变成固定边新增模型顶点。固定 160 面继续留在三维环带。先核对域内外、成对切缝身份及原始分段保持。
3. 只有运行入口和输入检查均通过，才最多调用一次受约束三角化；不细化、平滑、换点集或换工具重试。输出仅为相容三角底网，独立核查源绑定、固定点/边、接缝合并后拓扑、方向和 T 偶数性；任何项失败都停止，不进入四边配对/MILP、Blender 或生产门禁。

这一步可能推进到第一份新底网，尚不能承诺四边模型可行或角色即将完成。原生入口仍是独立未验证事项，本轮没有对它作新执行结论。

## 12. CHANGE-083：核心输入就绪，原生异常路径拒绝

### 12.1 两项独立结果

本轮按 §11.4 执行一次编译与一次独立输入准备。前轮来源检查保持完成，没有重跑切缝或边界，也没有更改模型阈值。

| 对象 | 实测结果 | 当前判断 |
|---|---|---|
| 已有原生工具入口 | GCC 13.2.0 + 本次进程的 Command Line Tools 选择，唯一一次编译成功；版本输出为 CGAL 6.2.1 | 旧的工具分派/编译阻塞取得进展，但不等于入口通过 |
| 正向合成输入 | 方形带中心点、凹形区域的输出通过身份/面积控制 | 仅支持这两份小样的正常执行 |
| 原生拒绝路径 | 重复点、固定边被拆分、非法点数、非法下标均返回 `-6`（SIGABRT），stdout/stderr 为空 | 未正常捕获并报告错误，原生入口拒绝 |
| 核心点输入 | 3,736 个平面点，一次准备完成并通过输入检查 | 可冻结复用，不能交给当前拒绝的入口 |
| 实际底网 | 真实输入三角化 0 次、新网格 0 份 | 未进行真实输出审计，模型质量未改善 |

### 12.2 一份冻结输入

点集合为 98 个固定边界副本、64 个切缝事件的 128 个成对副本、3,510 个原源内部点。合并明确身份后共 3,670 个不同身份，66 对别名包括两个固定锚点。原近端/公共环的 **96 段模型固定边**完整保留；新增来源点只细分计算切缝，因而平面外边界总计 226 段，不是把模型固定环拆为 226 段。

原源内部点按同一套严格域内规则选择，旧源接缝副本按原 ID 去重，物理原边界排除。切缝点来自 CHANGE-081 已登记转换，并投影到其明确公共源要素；保留来源记录和绑定误差，不冒充精确实数交点。固定点继续使用原 float32 字节，160 面固定带不送入平面三角化。

最大源绑定误差为 **0.000015178570 mm**，原 0.5 mm 阈值不变。独立读取保存文件后，检查成对 XYZ 字节、96 段固定边和精确有理数面积均通过，面积为约 0.7174427412791681。若全部点被使用，Euler 关系预测 T=7,244；这是推导值，**不是已生成三角数**。

冻结 `input.npz` SHA-256：`ba561a4ef14216279c1624275d1114bc3a281ae7c6fb457d85a91ff617a854de`。原生产合同、256 点和既有源/环带保持，完整自由域、底网几何、四边可行性、S0 和后续角色流程均未认证。

### 12.3 原生拒绝的证据与定向资料

本机系统默认开发目录仍指向 Xcode。通过仅在子进程设置 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`，已有工具可正常调用；这属于 Apple 支持的进程级选择方式，没有修改系统默认设置或接受许可。[Apple 设置说明](https://developer.apple.com/documentation/xcode/configuring-command-line-tools-settings)。原 CHANGE-079 仅保存错误摘要，不能补造当时完整内部调用栈。

本次实际 SIGABRT 栈包含 `libstdc++` 的 `read_encoded_value_with_base` 及系统 `libunwind`，把后续调查范围缩到 C++ 异常展开兼容性。尚未证明唯一根因，不能直接套用某个旧 GCC bug。编译还报告 11.0 部署目标与现有 13.0/14.0 库目标不一致的警告，本地二进制不宣称支持 macOS 11。

[CGAL 6.2.1 编译器支持表](https://doc.cgal.org/latest/Manual/thirdparty.html)列出 macOS GCC ≥13.3.0 和 Apple Clang ≥14.0.0，旧版本可能工作但无保证；本轮 GCC 13.2.0 低于其支持下限。本机 Apple Clang 21.0.0 已实际查询可用。[Apple C++ 支持说明](https://developer.apple.com/xcode/cpp/)说明其编译器配合 `libc++`。因此下一步有依据地调整同一 CGAL 的编译入口，不更换几何算法或重新选择模型点。

### 12.4 测试、预算和交付

初版联合测试报告为 14 项中 12 通过、2 失败。复核发现“非法输入”原断言仅要求非零退出，会错误接受 SIGABRT；已加强三个负向用例的断言，要求退出码 1 与错误信息。四项原生错误场景的诊断均确认 `-6`/空 stderr，拒绝状态保留，未弱化测试或重跑整套原生控制后冒称通过。最后一次 Python-only 运行 **12 项通过、5 项原生用例显式跳过**，不是 17 项全通过。输入准备后还补上必需来源路径/哈希覆盖的显式校验及 3 项控制，已有冻结 manifest 符合新增要求，没有重建点集。

06:24:57 UTC 开始；06:28:53 唯一编译完成；06:33 后原生错误控制拒绝；06:36:28 输入准备开始，06:39:09 独立保存文件审计通过，06:40:49 收敛前置结果，早于 06:54:57 前置截止。07:24:57 为归档/推送总截止。原生合成进程共 10 次（含 4 次诊断采集），另一次版本查询；真实三角化 0 次。没有第二次编译、MILP、Blender、付费或新依赖安装。

App 档案：`character_pipeline/sprout/v2/work/experiments/sprout-core-triangulation-20260922/`。手写 C++ 源码按编译哈希保存在 `native/`，明确为拒绝入口，未作为已验证工具发布；上游头文件、编译二进制和完整研究快照只在私有目录保留。531 项旧保护扩展为本轮 573 项，既有文件、用户根工作树和 App 原 4,725 个未跟踪路径保持。最终哈希/链接、双仓推送和工时见私有同名目录 `receipt.json`，不增加产品验收通过数。

### 12.5 下一项及停止条件

唯一下一项是：**已有 Apple Clang/libc++ 验证同一桥接，并复用本轮冻结输入**。不再调用 GCC 试旗标，不禁用异常，不重建点集或重跑来源追踪。具体参数见档案 `next-entry-plan.json`，本轮未执行：

1. 先补齐真实三角输出的独立审计与必要合成控制，覆盖固定点/边、成对接缝合并后拓扑、与原固定带接续、三角方向/退化、源绑定和 T 偶数性；这些不等于四边模型或完整 S0 审计。
2. 使用本机 Apple Clang 21、`libc++`、已有 SDK 26.5，明确 `x86_64` 以匹配现有 GMP/MPFR，部署目标与库对齐为 14.0。最多一次桥接编译；优先执行四个错误场景，须正常返回错误，再做正向小样。
3. 全部入口控制和输入指纹通过，才将本轮冻结 stdin 交给桥接一次；独立审计失败即停止，不细化、平滑、换点或更换三角化库，也不进入四边配对/MILP。

预算仍为 30 分钟前置、60 分钟总计；无需安装依赖或代用户接受许可。输入已经准备好，但不能把工具修复等同于未来底网必然通过或角色即将完成。

## 13. CHANGE-084：输出审计就绪，标准库头文件入口失败

2026-09-22，承接 §12.5 的用户继续授权。本轮没有重建输入或重跑边界来源；3,736 点 NPZ 仍为 `ba561a4ef14216279c1624275d1114bc3a281ae7c6fb457d85a91ff617a854de`，桥接源码 SHA-256 仍为 `cfa780b2d52155a3e2468c1bf52123a2e7b695ad3f4b68e5a5b640257a980508`。

### 13.1 交付与实际验证范围

App `5d8592c` 新增 `audit_sprout_core_triangulation.py` 及对应控制。检查器解析实际三角 ID，验证全部点使用、精确正向面积、原平面边界、连通性与顶点扇；按声明身份及 float32 字节合并接缝，再接原存档的 160 个有向四边面，检查 256 点和两端固定分段、环带拓扑及公共接口方向。最后核对源绑定、三角退化和参数重心处的源法向。检查器不会自动反转面、近邻焊接或移动固定点。

这些属于底网前置检查；重心法向抽查不能代替完整自交、双向贴合、四边配对或 S0 验收。真实输出未生成，故所有真实输出检查均未执行，不能把合成控制通过计为真实底网通过。

| 项目 | 实际结果 |
|---|---|
| 新输出审计控制 | 16 项通过 |
| 既有输入/拓扑控制 | 12 项通过，合计 28 项 |
| 首轮控制偏差 | 13 项中 12 过/1 失败；合成断言错误假定新增数字编号顺序，已改为按返回身份核对，并验证原面绕序 |
| Apple Clang 桥接编译 | 1 次，exit 1；`cstddef` 未找到 |
| 原生异常/正向控制 | 0 次，编译失败后未启动 |
| 真实三角化/输入重建 | 均 0 次 |
| 新底网/四边角色/模型改善 | 均无 |

### 13.2 已定位的编译阻塞

使用已约定的 Apple Clang 21、`x86_64`、`libc++`、SDK 26.5、部署目标 14.0 及原 Boost/GMP/MPFR；唯一编译在 07:38:47 UTC 失败。实际诊断为 Boost 包含标准头 `cstddef` 时找不到文件，不是 CGAL 几何求解失败。

后续只读检查确认：工具链的 `usr/include/c++/v1` 目录存在，但只有 4 个旧条目，没有 `cstddef` 或 `__config`；已有 SDK 26.5 的对应目录含 1,685 个头文件及全部本源码所需标准头。第一次 `-###` 仅打印分派，确认编译器选择了前一目录且没有选择 SDK 的 C++ 目录。

[Clang 官方参数文档](https://clang.llvm.org/docs/ClangCommandLineReference.html)提供 `-stdlib++-isystem` 指定 C++ 标准库头文件目录；[上游 Darwin 驱动源码](https://clang.llvm.org/doxygen/Darwin_8cpp_source.html)说明工具链目录优先于 SDK 的选择规则。上游源码用于解释机制，不冒充本机 Apple 分支的逐行证据。第二次只打印分派加入现有 SDK 路径，返回 0，确认该路径传入 `cc1`；没有执行编译器/链接器或生成二进制，不能据此认定修复成功。两份官方快照和摘要保存在私有目录，来源/哈希见档案 `header-diagnosis.json`。

### 13.3 保全与工时

07:28:13 UTC 起算；前置拒绝记录时间 `2026-09-22T07:39:42.754677+00:00`，早于 07:58:13 前置截止；08:28:13 为归档推送总截止。后续仅收敛只读路径诊断、补三项文件边界控制及落档。一次真实桥接编译、两次 print-only 驱动查询、零原生几何/求解/Blender/付费/安装/许可接受。默认开发目录未改。

[本轮 App 档案](../../../engineering/workspace/macos-app/character_pipeline/sprout/v2/work/experiments/sprout-core-clang-entry-20260922/README.md)保留执行参数、失败原文、28 项控制日志、输入指纹与未执行接续计划。603 项保护文件、原 256 点/75 mm 核心及实际一圈、合同和正式资源不变；用户根工作树与 App 原 4,725 个未跟踪路径保持。最终双仓远端、链接检查与工时见私有 `sprout-core-clang-entry-20260922/receipt.json`。

### 13.4 唯一下一项及停止条件

显式选择**同一个现有 SDK** 的 C++ 标准库头文件，复用原源码、原冻结 stdin 和已通过的输出检查器。`next-entry-plan.json` 绑定八份关键标准头 SHA-256；不更换编译器、SDK、GMP/MPFR 或点集。

1. 先核对标准头存在/哈希及驱动打印的 SDK、架构、头文件、链接器和 `libc++` 选择；最多一次实际桥接编译。
2. 编译通过后先执行非法计数、非法下标、重复身份、拆固定边四个错误场景，均须正常 exit 1、有诊断、无网格输出；再执行正向方形/凹多边形控制。
3. 全部前置与冻结输入指纹通过，才执行一次真实三角化并独立审计。编译、原生控制或真实输出失败均停止；不追加旗标试验，不重建点集/追踪、不细化/平滑、不启动 MILP、Blender 或付费。

30 分钟前置、60 分钟总预算不变。下一项是已定位路径修正的验证，不是模型已接近完成的证据；完整角色生产仍未完成，没有整体无解或必须人工接手的结论。

## 14. CHANGE-085：真实底网连通通过，三维面方向拒绝

2026-09-22，执行用户继续授权及 §13.4，并落实“改善不明显或陷入死角时自行反思和互联网搜索”的明确要求。App `d79578d` 保存本轮证据；输入 NPZ、stdin、桥接源码及原检查器哈希保持，不重建输入或重跑源边界。

### 14.1 原生入口已验证修通

09:00:01 UTC 起算。预检核对 3,756 份 CGAL 头文件、8 份 SDK 标准头、冻结输入指纹，以及打印分派的 x86_64 / SDK 26.5 / libc++ / CLT 链接器。以原源码加已登记的显式 SDK 标准头路径编译一次，09:02:21 exit 0；11 条 Boost 旧字面量声明弃用警告原样保留。

四类错误输入按顺序执行：非法计数、非法下标、重复身份、拆固定边，全部正常 exit 1、有诊断且 stdout 为空。随后方形和凹多边形通过独立面积/拓扑检查。六个原生合成进程全部通过；既有 28 项 Python 控制通过记录按未变代码哈希复用，本轮没有重跑或虚增为新通过数。二进制实际链接 `/usr/lib/libc++.1.dylib`，不含 GCC `libstdc++`；不声明 App 最低系统版本改变。

09:04:07 原生入口准入后，把同份冻结 stdin 交给桥接一次，约 0.25 秒返回 7,244 行实际三角 ID。编译入口不再是当前阻塞；无需下一轮重新编译或试 SDK。

### 14.2 独立真实输出结果与拒绝证据

| 检查 | 实际结果 |
|---|---|
| 平面盘 | 3,736 点 / 10,979 边 / 7,244 三角，Euler=1；全部点使用、226 原平面边界段保持；全部面积严格正且精确总和与原多边形相等 |
| 接缝身份合并 | 仅合并字节相同的声明身份；3,670 点 / 10,914 边 / 7,244 面，Euler=0，96 物理边界段 |
| 与固定带接续 | 3,830 点 / 11,234 边 / 7,404 混合面，Euler=0；原 160 个四边面绕序及连接保持，256 点 float32 字节不变；原 64/32 两端边界保持 |
| 源绑定最大误差 | 0.000015178570 mm，仍小于原 0.5 mm 限值 |
| 三角退化 | 存储坐标下零面积面 0 个；最小面积 1.909450457×10⁻⁸ m² |
| 重心源法向 | 15 个三角与对应源面方向不一致；最小余弦 −0.996927136；几何检查拒绝 |
| 完整自交、双向贴合、四边可行性、S0/身份/骨架 | 未通过；前两项本轮未执行，其余未进入 |

09:04:14 独立审计 exit 1，立即停止构造，未交换任何面、改点、细化或重跑底网。原始三角输出 SHA-256 为 `1084ef918aa80984db3293275da8b7726b6dfd1d4cb332e79f51a33de01d60c8`；拒绝的实际混合网格及身份映射也已保存。

对全部 15 个拒绝面做一次只读诊断，每面取 7 个预定重心权重位置。11 个面在全部 7 点方向不一致，7 个面接触冻结平面边界；异常涉及近端、公共环、切缝及内部区域。最大“直线三角插值点到同参数原源点”的采样差为 9.660030898 mm。它是对应关系诊断，不是最近表面距离或原合同双向贴合值；15 个异常也不是完整自交/折面计数。不能用 15/7,244 推算剩余工作百分比。

实质进展是首次有实际底网证明上述固定连接与拓扑可成立；仍没有合格三维底网或模型质量改善结论，不能与早期不同四边模型的失败数量直接比较。

### 14.3 主动复盘与互联网研究

本轮失败后即自行检索并读取原始资料，五份快照及哈希见档案 `research-sources.json`。复盘结论有两层：先前编译链检查拆散，造成多轮环境排障；当前“平面连接合法且顶点在原源上”也不足以保证连成的三维直面方向和贴合正确。

- [CGAL 6.2.1 CDT 定义](https://doc.cgal.org/latest/Triangulation_2/classCGAL_1_1Constrained__Delaunay__triangulation__2.html)约束的是平面边和空圆条件。结合本轮结果，同点集重复平面 CDT 没有修正源法向的新增机制，故不重跑。
- [CGAL 局部重网格接口](https://doc.cgal.org/latest/PMP_Remeshing/group__PMP__local__remeshing__grp.html)区分拆边、塌边、换边、松弛和投影；约束边、约束点及 `protect_constraints` 具有不同作用。借鉴可隔离的局部连接操作，不直接启动整套重网格或假定“保护特征”就满足原合同。
- [PMP 重网格说明](https://www.pmp-library.org/remeshing.html)建议出问题时分离操作定位原因。下一项据此只评估连接变化，不混入点移动、投影和密度变化；这不是该库保证修好本模型的证据。
- [Geometry Central 内蕴三角化](https://geometry-central.net/surface/intrinsic_triangulations/basics/)与[公共细分](https://geometry-central.net/surface/intrinsic_triangulations/common_subdivision/)明确维护到原表面的对应关系。公共细分产生交点，且要求原输入顶点均保留；标记边也可能被拆分，因此不满足本项目“固定段不拆”的直接替换条件。只借鉴来源对应关系的思路，本轮不安装或运行该工具。

当前选择是**先评估带原表面方向约束的局部连接替代**，而不是认为普通换边必然足够。若固定边界附近的源域表达使当前点集无法在限定邻域内通过，必须记录该局部障碍再研究，不扩大试修或推断完整合同无解。

### 14.4 下一项的可验证目标与停止条件

复用本轮真实输出和失败面 ID；冻结原坐标、256 点/96 物理边界段、声明接缝及 160 面环带，不调用编译或 CDT。30 分钟前置、60 分钟总预算，具体未执行计划见 `next-step-plan.json`。

1. 固定 15 个已发现异常面及其边相邻一圈，枚举未锁定内部边，每边只评估一次；计算切缝需成对处理或排除。先用合成控制验证检查器能拒绝固定段改变、点字节改变、重复对角线、非凸/反向平面替代和源法向失败。
2. 在同四个顶点上比较原/替代对角线，要求原域覆盖、两面平面正向、采样源法向正向及锁定边连接保持；独立记录源对应与直面误差，不偷换为完整贴合通过。
3. 必须报告每个异常面是否存在可用候选，以及候选之间是否冲突。前置内无法形成全部兼容覆盖则停止施工，保留明确障碍，不扩大一圈、不连续贪心换边或扫描参数。
4. 只有全部兼容覆盖成立，才最多组装一份仅修改连接的候选并独立审计一次，重查全部 7,244 三角、接缝/环带、固定字节及新增失败；任何失败停止。无点移动/增删、平滑、投影、完整重网格、MILP、Blender、安装或付费。

局部三角检查通过也不等于完整自交/贴合、四边联合约束或 S0 通过。完整角色后续仍需三维几何合格、核心四价/条带联合配对、全身接合及生产身份/骨架验收，不把当前 15 处当成全部剩余问题。

### 14.5 用户要求：后续自动复盘与检索规约

用户于本轮明确要求：陷入修复死角或改善不明显时，AI 应自行反思并搜索互联网方法，不等待用户再次提醒。后续执行遵守：

1. 一旦同类失败复现、原假设被证伪，或一轮没有新增真实验收通过项，先暂停同类构造；区分运行环境、输入、拓扑、几何和合同约束的阻塞层级。
2. 冻结失败输入、输出及见证，说明已证实什么、仍未知什么；工具测试、文档和脚本数量不计为模型改善。
3. 自动定向检索官方文档、作者论文/实现和可复现实例，核对固定点、固定边、源对应、面流等适用条件；成功演示不能直接视为满足本合同。
4. 在继续尝试前记录所选方法、淘汰理由、可测改进、最大范围与停止条件。没有新增证据，不重试同输入同算法，不用换密度/容差/工具掩盖停滞。
5. 汇报分别说明工具是否可运行、实际网格通过哪些检查、最终模型是否合格；明确失败和未测项，避免把局部进展说成即将完成。

### 14.6 执行与保全

09:00:01 开始，09:04:07 前置准入，09:04:14 真实审计拒绝，09:11:29 有界诊断/研究收敛，均早于 09:30:01 前置窗口。10:00:01 为总截止。一次编译、六个原生控制、一次版本查询、一次真实三角化及一次审计；零输入重建/源追踪/几何重试/新依赖/付费，不接受许可或更改系统开发目录。

[App 档案](../../../engineering/workspace/macos-app/character_pipeline/sprout/v2/work/experiments/sprout-core-sdk-entry-20260922/README.md)共 30 个文件，保存实际拒绝网格、原始三角 ID、审计及研究结论。625 项既有保护文件、用户根工作树及 App 原 4,725 个未跟踪路径保持；最终双仓远端、链接检查和工时见私有 `sprout-core-sdk-entry-20260922/receipt.json`。正式资源和原合同不变，无新产品验收通过数。

## 15. CHANGE-086：单次换边覆盖失败与采样点约束复盘

2026-09-22，按用户继续授权执行 §14.4。App `fdb157d` 新增纯连接核查工具、CLI、20 项合成控制和 21 份实验档案。只有全部异常得到兼容覆盖才组装候选；本轮不满足该条件，因此网格没有修改，没有新增模型验收通过项。

### 15.1 完整的有限核查结果

范围固定为原 15 个异常面及其边相邻一圈，共 55 面；原始关联边共 45 条，每条只评估一次。不把其他邻面间的边加入试验，不做连续换边。每个替代使用同四个原顶点，验证精确正向平面面积/同域、7 点源法向、明确身份合并后的拓扑和原 96 段边界；计算切缝边按锁定边排除。

| 分类 | 实际数量/结果 |
|---|---|
| 固定边锁定，禁止替换 | 5 条 |
| 另一对角线非凸或造成反向平面面片 | 19 条 |
| 平面可替换，但采样源法向仍失败 | 12 条 |
| 局部面积/法向/接缝拓扑可用 | 9 个替代 |
| 无候选的目标面 | 38、128、246、5263、6130、7243 |
| 全部兼容覆盖 | 失败；因 6 个目标没有候选，组合搜索为 0 节点 |
| 新三角化、组装网格、完整输出审计 | 均 0 次 |

20 项新控制覆盖固定/边界/接缝身份、重复对角线、非凸域、反向/退化法向、重心之外失败、float32 字节改变、候选冲突、搜索预算与输入/输出路径边界。最终 20 项全部通过；真实枚举单次完成。这证明检查器和有限结果可审查，不证明模型改善。9 个替代尚未写入网格，也不是“修好 9 处、只剩 6 处”。

### 15.2 只读原因核查

6 个无候选面中，5 个含原物理固定边，1 个在内部。对每面原定 7 点取重心方向步长 `1e-4` 的源映射有限差分，42/42 与原源面同向，且每次差分的三点都位于同一源三角内；这只排除了采样位置的局部方向异常，不能推为全域单射。

对这 5 条固定边各取 33 点：同参数源曲线与直线弦的最大差分别为 3.084944、2.040869、4.560052、0.199218、1.355324 mm；到完整 8,865 三角源图的最近距离采样最大值分别为 0.169044、0.086870、0.058040、0.193729、0.349701 mm。两种量明显不同，前者不能冒充最近表面距离。后一项仍不是连续界、限定自由域或原合同双向贴合验收。

9 个方向可用替代中有 3 个同参数差反而增大，其中一个达到 10.330691 mm。故“局部法向变正”不是质量通过，不能只按异常计数选择网格。当前有限证据未发现必须更改原固定边的理由；曲面对应与直线连接的差异，以及强制保留每个内部输入采样点，应作为下一项待检验假设。

### 15.3 自动互联网研究与方法选择

失败后自动执行 §14.5，保留四份官方原文快照及哈希。不同方法的适用性如下：

- [CGAL 曲面简化](https://doc.cgal.org/latest/Surface_mesh_simplification/)区分保留边和保留点位；借鉴保约束端点的有向收缩。标准简化不保证无自交，且针对旧面法向的相对变化过滤不能替代绝对源法向检查。
- [CGAL 局部重网格](https://doc.cgal.org/6.2/PMP_Remeshing/group__PMP__local__remeshing__grp.html)默认混合多种几何操作，不直接用于本轮冻结条件。
- [CGAL 多面体包络](https://doc.cgal.org/latest/Polygon_mesh_processing/structCGAL_1_1Polyhedral__envelope.html)提供整条线段/三角的容差包含检查思路；单向包含不等于原双向贴合及自交通过。本轮未运行包络算法。
- [Geometry Central 公共细分](https://geometry-central.net/surface/intrinsic_triangulations/common_subdivision/)用于对应关系会引入相交顶点，不直接替换原固定边分段。

不重跑相同 CDT 或原换边集合，不拆固定边，不启动默认简化/重网格流水线。新选择是**先评估删除少量指定非固定内部采样点的有向收缩**：保留端点采用原已有坐标，所有留下的点不移动。它是研究假设，还没有执行或证明可行。

### 15.4 下一项范围与验收

由 @winston（AI 执行）承担，30 分钟前置、60 分钟总预算。下一次先登记新 CHANGE，明确修订的是本试验“全部 3,736 输入点必须保留”的附加条件；原 256 点 float32、96 个物理固定段、160 面环带及生产合同不变。不得以简单关闭全部点检查来放行删除。

1. 仅列入 `source:46/172/174/2857/2864/2919/3283/3677` 八个可评估删除的非固定内部点；其原始点星形邻域共 42 面，名单及原面 ID 已冻结于 `next-step-plan.json`。这表示可评估对象，不是必须删除八点。
2. 每个原有向相邻边只评估一次，保留端点为原有点；固定点、物理边界点和切缝点均不得删除。先通过合成控制，再检查链接条件、精确域覆盖、正面积、绝对源法向、身份接续及全部保留点字节；独立报告最近源距离。
3. 与本轮已有换边候选组成有界兼容覆盖；目标仍是原 15 个异常。不允许自动扩大点集、连续贪心简化、点移动/新增、拆固定边、投影或参数重试。覆盖不足或前置超时即停止构造并复盘。
4. 只有完整覆盖成立且独立审计已能核对明确删除账本/旧新 ID 映射，才最多组装一份候选；重查全部实际面、原固定带及源对应。前序几何通过后按原采样完成完整自交/双向贴合，可复用已安装 Blender 做只读审计。任何失败不做第二份候选。

这一方法不保证收缩后贴合或后续四边联合约束可行；全身接合、核心四价/条带、身份、骨架和 S0 仍需各自验收。尚无整体不可修复或必须人工接手的结论。

### 15.5 工时与保全

09:21:01 UTC 开始，09:29:31 完成有限枚举并拒绝覆盖，09:47:36 收敛原因与方法决策，早于 09:51:01 前置截止；10:21:01 为总截止。本轮只有一次真实连接枚举、20 项新合成控制及有界只读诊断。零编译/CDT、点移动/增删、新网格、Blender、MILP、安装或付费。

[App 档案](../../../engineering/workspace/macos-app/character_pipeline/sprout/v2/work/experiments/sprout-core-local-connectivity-20260922/README.md)保存 21 份文件及全部逐边见证。661 项既有保护文件哈希、用户根工作树、App 原 4,725 个未跟踪路径保持；系统开发目录、合同和正式资源不变。最终双仓推送、链接检查与工时见私有 `sprout-core-local-connectivity-20260922/receipt.json`。

## 16. CHANGE-087：有限收缩有局部改善，候选仍未通过

2026-09-22，按 §15.4 执行。App `17763d1` 新增六个收缩/组装/独立审计及控制文件，最终 30 项新增合成控制通过，保存 41 份证据。只组装一份候选；本轮可以确认方向指标有实际改善，不能认定整体质量通过或接近完成。

### 16.1 有限范围与唯一候选

八个允许评估的内部源点及原 42 面点星形邻域保持冻结，47 条原有向边每条评估一次：2 条链接条件失败、10 条精确平面方向失败、18 条源法向失败、17 条局部可用。与此前 9 个换边选项合并后，有界兼容覆盖在 15 个搜索节点完成；唯一组合使用 6 个收缩和 9 个换边。

明确删除的原 disk ID 及保留端点为：239→238、2836→2784、284→53、3179→63、286→39、3462→67。删除的是登记的内部源采样点；收缩至已有相邻点，全部留下的坐标字节不变。删除账本、旧新点 ID、原面对应及文件哈希由独立审计核对，未关闭固定点、边界或全部保留点使用检查。

计算域从 3,736 点/7,244 三角变为 3,730 点/7,232 三角；接缝合并后自由核心为 3,664 点/7,232 三角，接入原 160 面环带后为 3,824 点/7,392 混合面。精确平面面积、226 条计算域边界、Euler/接缝连接、256 个固定点 float32 字节、96 条物理边界及 160 个定向固定四边面保持。源绑定最大误差 0.000015179 mm，三角退化 0 个。它仍是中间三角底网，不是合格四边模型。

### 16.2 同口径真实进展与剩余问题

候选通过原重心检查后，独立审计继续检查每面固定 7 点；发现失败即拒绝，没有组装第二份候选。为防止新旧检查口径混淆，又对原 CHANGE-085 网格执行同一 7 点规则，结果如下。

| 同一检查项 | 原冻结网格 | 本轮唯一候选 |
|---|---:|---:|
| 重心源法向异常面 | 15 | 0 |
| 每面 7 点中至少一处异常的面 | 30 | 14 |
| 7 点规则下异常采样点 | 119 | 21 |
| 7 点最小源法向余弦 | −0.999138176 | −0.843144573 |
| 同参数点差最大值 | 9.660031 mm | 10.330691 mm |
| 完整自交 / 原双向贴合 | 未执行 | 未执行 |

剩余 14 个原面 ID 为 4108、4120、4139、4157、4316、4359、4403、4900、4989、5044、5157、5234、5386、5466；全部与原网格逐面相同。本轮消除了 16 个原 7 点异常面，没有新增该类失败，但不能据此宣称其他质量没有退化。同参数差反而增大，也不能将其误写为最近表面距离或原合同贴合结果。

这些面均位于内部，7 点到完整源图的单向最近距离采样最大为 0.614120 mm。采样源法向之间的最小余弦范围为 −0.565191 至 0.873984，最长边/对应高比为 3.076598–17.282106；前者不是精确曲率，后者不是四边面长宽比门禁。所有量均为诊断，不能替代全网自交、双向覆盖或连续方向证明。

### 16.3 自动反思与原始资料检索

按 §14.5 主动复盘：原先只将重心发现的 15 面列为修复目标，会漏掉面内其他位置已有的异常。因此下一项先获得完整障碍清单，再决定是否继续局部构造，不能把当前 14 面当作所有剩余问题。

- [CGAL 曲面简化](https://doc.cgal.org/latest/Surface_mesh_simplification/)将收缩代价、保留点位和停止条件分开；可借鉴有限有向收缩，普通简化仍需额外几何过滤。
- [CGAL 自适应边长](https://doc.cgal.org/latest/PMP_Remeshing/classCGAL_1_1Polygon__mesh__processing_1_1Adaptive__sizing__field.html)结合局部曲率和容差选边长；它提示不同区域可能需要不同处理，不能据当前诊断直接决定统一删点或加密。本轮未引入 Eigen 或运行重网格。
- [CGAL 多面体包络](https://doc.cgal.org/latest/Polygon_mesh_processing/structCGAL_1_1Polyhedral__envelope.html)提供完整线段/三角误差包络检查；单向包含不证明反向覆盖、无自交或源法向一致。本轮未运行该方法。

三份官方原文快照与哈希保留；这些资料支持方法选择，不是本模型通过证据。不继续同一组收缩、不直接扩展到另外 14 面、不用密度/容差扫描代替诊断。

### 16.4 下一项：只读整网几何对照，零新候选

由 @winston（AI 执行）承担，30 分钟前置、60 分钟总预算，开始时登记下一 CHANGE。只读取两份冻结混合网格：CHANGE-085 原网格和本轮拒绝候选；各执行一次相同规则的整网检查。前置需核对输入/源图/删除账本哈希，确认已安装 Blender 的版本与指纹，并以合成控制验证混合三角/四边面的采样适配；不安装依赖。

1. 复用既有非相邻面自交检查，保留原始面配对；注明共享顶点的面被排除，不能冒充完整连续无自交证明。同时检查退化、固定四边折面、连通性及固定边界。
2. 正向使用所有点、唯一边中点、面中心；四边面沿用 4×4 双线性采样，三角面在执行前写明唯一的 16 点面积均匀重心采样规则，固定后不扫描密度。反向使用同份原源图面中心和边中点。
3. 同时报告两个方向的 P95≤1 mm、最大≤2 mm 原门槛，最差位置和原面/源面身份；有限采样限制必须保留。将失败位置与已知 14 面及本轮改动面对应，判断局部改动是否有范围外副作用。
4. 本项新候选上限为 0；不收缩、换边、移动/新增点、投影、平滑、重跑编译/CDT、MILP、付费或安装。输入/运行时/格式前置失败即停止，不换工具继续试。若发现更广的自交或贴合阻塞，先记录真实原因，再另定构造范围。

本候选持续标记几何拒绝。完整身体、四边联合约束、全身接合、S0、生产身份、32 骨及动画均未完成；没有整体不可修复或必须人工接手的结论。

### 16.5 工时、证据与保全

09:57:09 UTC 开始；10:05:00 完成有限枚举；10:10:08 最终控制及唯一候选完成；10:10:49 全网方向审计拒绝；10:12:59 完成新旧同口径对照，均早于 10:27:09 前置截止。拒绝后的只读研究与下一项决策于 10:32:01 记录；总截止为 10:57:09，不把研究完成时间写成前置内完成。

本轮最终 30 项新控制通过，六点删除/九次换边只写入一份候选；无保留点移动、新增点、编译/CDT、Blender、MILP、安装或付费。711 项原保护文件、用户根工作树、App 原 4,725 个未跟踪路径、系统开发目录、生产合同及正式资源保持。最终双仓推送、链接检查与工时见私有 `sprout-core-local-contraction-20260922/receipt.json`。

[App 档案](../../../engineering/workspace/macos-app/character_pipeline/sprout/v2/work/experiments/sprout-core-local-contraction-20260922/README.md)保存 41 份文件，包括实际拒绝网格、删除/面对应账本、全网审计、新旧同口径数据、研究记录及未执行的下一项计划。

## 17. CHANGE-088：整网对照揭示反向覆盖缺口

2026-09-22，按 §16.4 只读比较 CHANGE-085 原网格和 CHANGE-087 拒绝候选，每份各一次；本轮没有生成或修改网格。App `651be9a` 新增五个采样/审计/精确证书及控制文件，24 项新控制通过（18 项 Blender/采样、6 项精确下界），归档 38 份证据。上一轮局部方向及非相邻面自交改善属实，但反向贴合退化，不能采用候选。

### 17.1 同规则整网结果

| 检查项 | CHANGE-085 原网格 | CHANGE-087 拒绝候选 |
|---|---:|---:|
| 非相邻面自交对 | 1 | 0 |
| 退化 / 重复 / 固定四边第一对角线折面 | 0 / 0 / 0 | 0 / 0 / 0 |
| 网格→源采样数 | 140,932 | 140,704 |
| 网格→源 P95 / 最大距离 | 0.210350 / 1.084503 mm | 0.208874 / 1.084503 mm |
| 源→网格采样数 | 35,460 | 35,460（完全相同的源样本） |
| 源→网格 P95 / 最大距离 | 0.340007 / 4.017495 mm | 0.340039 / 4.171724 mm |
| 源→网格超过 2 mm 的样本 | 9 | 9 |
| 沿用前轮每面 7 点方向失败 | 30 面 / 119 点 | 14 面 / 21 点 |

两个方向均保持 P95≤1 mm、最大≤2 mm 门槛。正向采样通过不能抵消反向失败；9 个旧超限样本中 8 个误差增大、1 个不变，没有新进入超限或被解决的样本。原自交对为原面 4098 / 4195，候选未再检测到该非相邻面交叉。

两份数据均为单连通分量，原 64/32 两开口、96 段物理边界、256 固定点 float32 与 160 个定向固定四边面保持。三角中间网格的价数、三角长宽比不能用于宣称全四边/核心四价/条带门禁通过。

正向包含全部顶点、唯一边中点、面中心，每面固定 16 个内部样本；四边面逐值保留原 float32 双线性规则，三角面使用执行前冻结的 4×4 Duffy 面积均匀权重。反向沿用原源图面中心和三条边中点，含共享边重复样本。距离是有限采样；BVH 自交沿用 1e-7 m 容差，排除所有共享顶点的面；固定四边折面沿用 0–2 对角线，尚不能声称完整连续无自交或全部生产四边认证。

### 17.2 三处边界转角与额外退化

9 个超限样本含 8 个唯一 XYZ，集中于原裁剪边界三处转角；对应固定边 26–27、39–40、52–53，最近原面分别为 25、235、341。它们均不在剩余 14 个方向异常面中。因此“处理完 14 面即可合格”没有依据。

面 25 本轮及上一轮均未改动；面 235 与 341 在上一轮分别受 source:174→fixed:39、source:172→fixed:53 收缩影响。新网格的九个最近点均落在这三条原固定直边上（数值差约 0.00002 mm 内）。这说明当前反向覆盖缺口集中于边界邻域，不证明固定边必须改变，也不能用只看网格→源的结果掩盖缺失的源表面区域。

### 17.3 精确下界：停止当前点集的纯连接路线

针对最差的冻结源采样 9104，选定一个支持法向 n，分别计算全部原/新顶点的最大投影 h=max(n·v)。所有只使用相应点集的三角、双线性四边或凸组合式细分点，都满足 n·x≤h。源采样 p 若在另一侧，距离平方下界为 (n·p−h)²/(n·n)。

对存储的二进制坐标和采样值使用精确有理数，直接比较该下界与 (2/1000)²；原/新显示下界分别为 **3.847705 mm / 4.171730 mm**，均严格超限。结论不依赖某次换边搜索是否充分：当前这两份点集上继续只换连接、删点、插入现有点的凸组合，无法通过这一处最大误差检查。

这不是整体无解证明。它未排除从原曲面新增位于当前凸包之外的非固定点、移动非固定点或另设接口；也没有授权改变原固定边界。当前路线应从“重排现有点”转向核查缺失的源几何支撑，不能重复相同点集的 CDT 或简化。

### 17.4 自动研究与下一项范围

按 §14.5 自动复盘并保存三份官方原文：

- [CGAL 距离函数](https://doc.cgal.org/latest/Polygon_mesh_processing/group__PMP__distance__grp.html)区分单向/对称采样与有界误差距离，支持保留两个方向的独立结论。已有明确超限见证，本轮不另行编译距离器。
- [CGAL 包络过滤器](https://doc.cgal.org/latest/Surface_mesh_simplification/classCGAL_1_1Surface__mesh__simplification_1_1Polyhedral__envelope__filter.html)可拒绝包络外局部三角，但不能单独保证旧源域被新网格覆盖；不能替代反向贴合检查。
- [CGAL 局部重网格](https://doc.cgal.org/6.2/PMP_Remeshing/group__PMP__local__remeshing__grp.html)区分约束点、约束边和 protect_constraints。普通约束边仍可拆分/收缩；保护长边又限制目标边长。默认拆边/平滑/投影流程不符合当前冻结条件，未调用。

下一项由 @winston（AI 执行）承担，30 分钟前置、60 分钟总预算，新候选上限 **0**。先登记下一 CHANGE；只做六个原源内部参考点的有限准入与固定接口兼容性核查，不直接构造模型：

1. 冻结原 chart 面 **10、59、78、79、169、172** 的重心，保留原源面/重心权重、UV 和拟议 float32 坐标。这六个点到九个失败样本的最近距离最大为 **1.958899 mm**；这里只说明参考位置可覆盖已知空间缺口，点未写入网格。
2. 合成控制通过后，每点只查一次：源绑定、源参数逆映、计算自由域严格内部性、原包含三角，以及同三角直接局部分割的精确正面积/原 7 点绝对源法向。边界/接缝点不准入；若多个点属于同一原三角，报告冲突，不贪心插入或扩大空腔。
3. 只在临时数组中评估局部提案；检查有无兼容子集能让九个已有超限样本到拟议点均≤2 mm，不冒充完整表面贴合。明确三条固定边与相邻自由面的源方向/覆盖关系，保留 256 点、96 原段、160 面不变。
4. 如准入失败、前置超时或必须改动固定段，停止并记录具体见证，不调密度/容差、另选点位或扩大邻域。任何边界合同修订都要先形成有证据的独立方案。若六点准入成功，也只输出可行性表；后续联合施工仍必须包含原 14 面方向问题和整网门禁。

无编译/CDT/MILP、付费或依赖安装，不开放身份、骨架或动画。本轮新网格 0，完整身体/四边联合约束/S0/生产身份/32 骨与动作均未完成；没有必须人工接手的结论。

### 17.5 工时与保全

10:45:25 UTC 开始；10:56:29 前置通过；10:56:45 两份真实网格各一次检查完成；11:01:22 精确证书完成；11:06:06 记录复盘及下一项决策，均早于 11:15:25 前置截止。总截止为 11:45:25；最终双仓推送和实际工时见私有 `sprout-core-global-geometry-20260922/receipt.json`。

805 项原保护文件、用户根工作树、App 原 4,725 个未跟踪路径、系统开发目录、合同与正式资源保持。Blender 仅用于版本读取、合成控制和只读几何计算，没有保存场景或模型；现有两个审计函数文件未改。

[App 档案](../../../engineering/workspace/macos-app/character_pipeline/sprout/v2/work/experiments/sprout-core-global-geometry-20260922/README.md)保存 38 份文件，包括原始距离数组、完整自交对、九点边界定位、精确分离证书、来源明确的六个参考点及下一项未执行计划。

## 18. CHANGE-089：六个源点均在固定边旁发生方向冲突

2026-09-22，按 §17.4 完成六个冻结原源重心各一次的只读准入；App `3aabe0f` 新增检查器及合成控制两个文件，18 项控制通过，归档 18 份证据。没有创建、修改或保存网格。**本轮模型没有改善；六种直接接入方案全部拒绝，失败集中在三条固定边旁。**

### 18.1 来源与平面检查通过，三维接入失败

| 源 chart 面 | 原源面 | 原包含面 | 原固定边 | 反向子面采样 | 最小法向余弦 |
|---:|---:|---:|---|---:|---:|
| 10 | 98439 | 235 | 39–40 | 7/7 | -0.999083 |
| 59 | 99116 | 25 | 26–27 | 7/7 | -0.999821 |
| 78 | 99292 | 341 | 52–53 | 7/7 | -0.999609 |
| 79 | 99292 | 341 | 52–53 | 7/7 | -0.999962 |
| 169 | 99944 | 341 | 52–53 | 7/7 | -0.999271 |
| 172 | 99961 | 235 | 39–40 | 7/7 | -0.998326 |

六点均保持冻结原源面、裁剪面重心权重、UV、原源重心及 float32 位置。通过已知 UV 逆映并撤销原切缝平移，没有最近点投射。往返仍定位原 chart 面，最大绑定误差 **0.000015156945 mm**；全部严格落入唯一计算三角内部，没有边界、接缝、重叠歧义或容差挪点。

每点仅在临时数组中将原包含面拆成三个子面，存储二进制坐标的精确有理数面积全部为正，且和原面积完全一致。但接着使用原每面七点绝对源法向规则：每项的固定边相邻子面 **7/7 为负**，另两个子面均 **7/7 为正**。原三个包含面在同规则下全为正，所以不是把整个区域的绕序统一写反。42 个反向样本来自六份彼此独立的临时提案，不能合并成一张实际模型的指标。

10/172 共用原面 235；78/79/169 共用原面 341，合计四对原面冲突。未执行贪心串行插入或扩空腔。零点准入，因此不存在能覆盖原九个反向距离失败样本的准入兼容子集。上一轮六点位置本身的 1.958899 mm 覆盖线索没有转化为可用网格。

### 18.2 结论边界与主动复盘

关闭这六个点的原包含面直接分割路线，不改点位、容差或邻域重试。仍保留两份旧点集的精确距离下界结论；原候选的 14 面方向异常与九点反向贴合失败均未解决。当前结果支持优先检查固定直边对源边界转角的近似，但**没有证明任意保持现有接口的重建都不可能，也没有证明必须人工建模**。

已主动查阅并保存四份原始资料与 SHA-256：

- [CGAL 参数化](https://doc.cgal.org/latest/Surface_mesh_parameterization/index.html)：不同映射的保证受边界和权重条件限制。对本项目的推论是：原源三角的参数映射有效，不保证替换成新三维直边后仍为有效曲面。
- [CGAL Mesh_2](https://doc.cgal.org/latest/Mesh_2/index.html)：约束由直线图定义，细化可拆分约束段。共线拆段仍是同一几何直线，不能恢复缺失的源转角；使用原源曲线的分段表示需要明确记录段几何变化。
- [CGAL 局部重网格](https://doc.cgal.org/6.2/PMP_Remeshing/group__PMP__local__remeshing__grp.html)：保护约束与普通约束不同，默认流程可能改段。未调用 remeshing 或切换保护选项尝试。
- [Lipman 原论文摘要](https://arxiv.org/abs/1310.0955)：单元方向条件不足以单独保证单射，还需要边界/全局条件。本轮只读摘要，未复现定理或证明当前输入满足其假设，不以其宣称三维全局无解。

### 18.3 下一项：三段源边界表示与对接复核

由 @winston（AI 执行）承担，30 分钟前置、60 分钟总预算，零新候选。先登记下一 CHANGE，复用 CHANGE-082 已完成的边界追踪，不重跑六点或 96 段来源：

1. 只读提取 **26–27、39–40、52–53** 的原源区间链和源顶点事件，量化完整源折线到各自原固定直线段的最大距离。此单向曲线到一段距离在每个源线段上是凸函数，最大值在其端点；端点数值误差另列。边界曲线误差不能冒充完整曲面距离。
2. 列出被直线略过的源转角及方向关系。以已追踪源折线事件形成有限的边界表示提案，保留原固定端点坐标，列明新增身份、真实段变化和点/段计数；该表示只给数量上界，不冒称最少点或全四边可行。
3. 同步评估接口两侧、64/32 开口、全四边所需的边界偶数条件、160 面固定带、受保护核心及实际一圈。不得只修一侧留下裂口，也不能忽略原 14 面方向问题。
4. 输出具体推荐、需要变更的合同条目及后续有界验证方案。**本次复核不执行边界合同变更**；若确有必要，待方案具体可审查后再单独决定。来源、对接影响或拓扑范围仍不清楚时，明确缺项，停止，不再排一轮泛化补点。

不调用构造/编译/CDT/MILP/Blender/安装/付费或骨架。不改变质量门槛。下一项是对当前边界表示的设计复核，不是未经授权修改固定接口。

### 18.4 验证、工时与保全

11:32:53 UTC 开始，11:42:45 前置通过，11:43:02 单次六点检查完成，11:47:00 记录复盘决策，均早于 12:02:53 前置截止；总截止为 12:32:53。最终双仓推送、文档检查和实际工时见私有 `sprout-core-source-admission-20260922/receipt.json`。

18 项合成控制覆盖严格内部/边界/共边/重叠拒绝、极小浮点偏移不挪点、精确面积、非重心法向异常、源身份及逆映、同面冲突/至多六点子集、既有输出与冻结计划漂移。真实输入只运行一次。892 项保护文件、原 256 点/96 段/160 面、合同、正式资源、用户根工作树及 App 原 4,725 个未跟踪路径保持。没有产品测试通过数或 S0/身份/骨架/动画状态变化。

[App 档案](../../../engineering/workspace/macos-app/character_pipeline/sprout/v2/work/experiments/sprout-core-source-admission-20260922/README.md)保存 18 份文件，包括原始六点结果、固定边失败分类、研究来源、停止决策和下一项未执行计划。

## 19. CHANGE-090：四点局部边界提案，尚未执行网格修改

2026-09-22，按 §18.3 完成三段来源表示与双侧影响的只读复核。App `7af2ec4` 新增四个检查/设计及控制文件，28 项合成控制通过，28 份证据归档。**取得的是边界方案进展，没有新的模型质量改善。** 不再在跨越源转角的原直边旁继续插内点；下一项需明确修订这三段局部实验接口。

### 19.1 原直段确实略过源转角

复用 CHANGE-082 的 25 个来源区间、22 个内部事件，不重跑追踪或增加来源样本。对每条已存储源线段，距离到固定直段是凸函数；逐个事件的精确有理数平方距离因此给出完整源折线的单向最大值。

| 原固定段 | 来源区间 / 内部事件 | 曲线→原直段最大距离（mm） | 最大差对应裁剪源顶点 | 推荐新增顶点 | 新折线↔原曲线上界（mm） |
|---|---:|---:|---:|---|---:|
| 26–27 | 8 / 7 | 2.715163254 | 105 | 105、168 | 0.023353124 |
| 39–40 | 10 / 9 | 2.884519964 | 22 | 22 | 0.175758716 |
| 52–53 | 7 / 6 | 4.595623109 | 123 | 123 | 0.051040520 |

源 ID 指裁剪网格事件，不是原始 char1 顶点编号。源端点继承数值不确定度最大约 5.05×10⁻⁹ mm，另列并计入新上界；表中新上界向上取末位。新上界通过每条保留直段与其完整源子链的连续对应证明：首尾参数固定为 0/1，中间点取夹紧投影，线性插值位移不超过端点最大值，连续对应覆盖整段；计算保留精确分数。**这是边界曲线界限，不是整网最近曲面误差，更不是全局无解证明。**

### 19.2 推荐四个已有事件，明确局部接口范围

第一次完整表示保留全部 22 个事件，只给数量上界：近端 64→86、全部固定点 256→278。随后按事先冻结的规则只形成一次简化设计：保留 x/z 裁剪平面切换的三个真实转角 105、22、123；因三点会使总边界 96→99 为奇数，按 `4F=2E内部+B` 的纯四边必要条件再加一个已有事件。选最长保留源弧中距弧长中点最近的 168，长弧为 10.018310435 mm；不根据误差重新选点或扫密度。

拟议有向链为 `26→105→168→27`、`39→22→40`、`52→123→53`，新增 ID 在文件中使用 `boundary-source:` 前缀，与原 `fixed:` 身份区分。具体 float32 坐标及来源见档案 `interface-proposal.json`。

- 这是 **肩部参考补片近端局部切口** 64→68 点，远端 32 点不变。它不是生产角色的脖子接口。
- 原 256 个固定点全部按原字节保留；新增四点后拟议固定点总数 260。三条旧段改为七段，其余 93 段不动，总物理边界 96→100。
- 160 个固定四边面保持。22 个来源事件距核心中心至少 122.609594 mm，均在 75 mm 核心球外；三个当前相邻面不碰当前核心或实际一圈。未来仍须按最终连接重算一圈，不能因此宣称核心拓扑通过。
- 生产机器合同的脖子 64 / 腕踝 32、绑定及双向贴合门槛、四边比例、核心价数和零几何缺陷等要求全部不变。没有改现有合同、源资产或正式资源。

`proposed-local-interface-policy.json` 是**未生效提案**。后续需在新 CHANGE 内显式修订本设计 §3 / 局部迁移输入中这三段固定边条件，并使用新接口审计；禁止修改历史检查器伪装原 64/32 接口保持。偶数边界仅为必要条件，四点不是数学最小解证明，也没有全四边可行性保证。

### 19.3 对接两侧：来源存在，正式外侧补片尚缺

25 个来源区间的原始承载三角全部跨越对应切平面，共涉及 22 个不同原始面，说明原参考表面在切口两侧有材料。没有已制作、已批准且绑定到本试验的外侧生产网格；原 char1 只是研究参考，不能直接冒充已通过身份验收的全身。

外侧补片将来必须与上述三条链共享相同身份及 float32 坐标、采用相反遍历，并同时验证接缝及两侧表面。缺少外侧成品不阻止单独批准后的局部隔离验证，但阻止声称全身装配可用。原 14 个方向异常面、九个反向贴合失败样本及生产全身工作均保留。

### 19.4 原始资料与停止旧路线

主动查阅 [CGAL Polyline Features](https://doc.cgal.org/latest/Mesh_3/classCGAL_1_1Mesh__domain__with__polyline__features__3.html)、[MeshDomainWithFeatures](https://doc.cgal.org/latest/Mesh_3/classMeshDomainWithFeatures__3.html) 与 [Polygon Mesh Processing](https://doc.cgal.org/latest/Polygon_mesh_processing/index.html)，保存来源及快照哈希。借鉴的是角点/有向曲线/相邻曲面片的明确身份，以及组合一致性和几何有效性分开检查；未调用 CGAL 新算法。结合本地数据推导四点提案，不能将文献当作本模型成功证据。

当前停止固定直边旁的内点补洞，也不在下一轮失败后自动切换 22 点加密方案。没有“任意固定接口都无解”或“必须人类建模”的结论。

### 19.5 待用户确认后的单次边界验证

本轮授权是只读复核，尚未允许更改三条局部固定段。完整可审查方案及未执行的 `next-step-plan.json` 已落档；需要确认的是这一具体接口变更。相关项目 Skill 的证据规则要求 “Approval is required for a contract change”；不是要求用户找人工建模师。

确认后由 @winston（AI 执行），先登记新 CHANGE 并绑定本提案/局部规则哈希，30 分钟前置、60 分钟总预算，最多一份 `scoped_wip` 三角中间件：

1. 仅取原相邻面 **25、235、341**，用新链逐段连接各自不动的原对顶点，拟为七个替换三角、四个新边界点；不扩大空腔、换点或优化参数。
2. 构造前检查四点绑定、原固定字节、其余 93 段、160 面固定带、68/32 有向环；要求精确平面面积为正，每个新面的原七点源法向全正，并检查与未改面的新增交叉。任一失败停止，没有备选扇形或 22 点回退。
3. 前置全通过才组装一次；全部其余点/面字节及身份保持。原 14 个方向异常面须按原位置报告且不新增异常，此中间验证不算整网几何通过。
4. 按 CHANGE-088 相同整网采样比较双向贴合、自交、退化和固定带折面；另列全部原源顶点距离（含三个真实转角），补充旧反向采样仅用面中心/边中点的覆盖盲区，不混改旧指标。新增阻塞或反向覆盖没有改善即隔离结果、复盘研究。
5. 不制作或焊接外侧生产补片，不进入正式 S0/身份/骨架；全四边质量和最终核心实际一圈须在相应完整构造后另验。无 CDT/MILP/编译/安装/付费/阈值调整。

### 19.6 验证、工时与保全

11:54:02 UTC 开始，12:01:54 前置通过，12:02:13 完成一次三段核查，12:07:10 完成一次四点设计，12:10:46 记录决策，均早于 12:24:02 前置截止；总截止 12:54:02。28 项合成控制覆盖精确距离、连续来源、方向判断、裁剪角点、偶数条件及连续双向界限，不计为产品验收。936 项原保护文件、用户根工作树及 App 原 4,725 个未跟踪路径保持；零新网格/点插入/源采样/Blender/编译/CDT/MILP/安装/付费/骨架。

[App 档案](../../../engineering/workspace/macos-app/character_pipeline/sprout/v2/work/experiments/sprout-core-boundary-representation-20260922/README.md)含 28 份计划、原始数据、局部接口提案、研究、控制及哈希清单。最终双仓推送、文档链接及实际工时见私有 `sprout-core-boundary-representation-20260922/receipt.json`。

## 20. CHANGE-091：先验证路线必要条件

本轮没有构造网格。实际合并网格包含 3,824 点、7,232 三角和 160 固定四边面；2,783 个核心点加不可删除边连接的一圈共 2,815 个必保护点，其中 86 个核心点无法仅靠三角配对达到四价。独立枚举固定导向点 `fixed:832` 的相邻配对，最终最小度为 5，要求为 4。

这排除当前固定三角连接的配对输入，不能外推为其他三角化或重拓扑不可行。四点提案的三处原面没有涉及任何上述失败点，即使局部边界修好也不能消除它们；不再先做边界修补再尝试这份底网的配对求解。14 个异常面同时有正负源法向采样，单纯反转面朝向也被排除。

获批 Blender Studio 原件恢复且 SHA 一致，旧 intake 通过；源接口 36/22/28 与正式 64/32 不符，旧适配路线仍关闭。当前页面 CC-0 与旧冻结许可证据不同，分别记录。八类问题的证据层级、文献与后续具体布局交付要求统一见[八类问题的方法与验证清单](./pet-character-method-validation.md)，不重述为模型改善。
