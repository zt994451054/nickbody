# 膝部与肩臂结构方案选择

> CHANGE-063 / CHANGE-064 / CHANGE-065，2026-09-14，负责人 @winston（AI 执行）。
> **当前：B1 失败并关闭；后续生产路径决策已完成，见 §11 及[完整重建工单](./pet-character-production-rebuild-decision.md)。当前缺少完整实际布局与已确认建模能力，不启动新自动施工。**
> §1–7 保留 CHANGE-063 结构比较历史；§8 记录首次施工前的范围校正。原纯权重路线仍关闭。
> 本次固定点位的换边/配对没有形成合格关节面流，不能推广为所有定向重拓扑都无效。

## 1. 为什么选择这个方向

骨位、rest 姿态和表面连接都可能影响压缩变形，现有证据支持的程度不同：

| 方案 | 直接证据 | 决定 |
|---|---|---|
| 调整膝骨中心或 rest | CHANGE-034 已做单姿势虚拟中心后移 0.04：所查左腿相交从 30 降为 0，但相邻折叠仍在；前移变为 208。原左腿选面未单独序列化，数字不是当前完整门禁。当前骨链端点连续性也没有给出中心/轴错误的证据 | 本次不选；没有排除其他骨位设计的可能性，但没有支持下一次具体移动量的依据 |
| 定向重建局部面流 | 已定位的肩臂九面在 rest 就有细长三角形，跳跃时局部面积进一步压缩；同一小簇同时参与目标穿插与误差敏感折角。膝部也有明显压缩及折回，原尺寸图可见不同高度表面挤在同一折线上。已能从原连接定义完整目标的施工包络 | 优先做一次真实施工验证；验证对象是新的连接和相应权重，不能靠更换求解器宣称方案已变 |
| 更换整份源资产、全局重网格或重做骨架 | 尚未验证本次明确包络内的定向施工；会扩大身份、UV、绑定和动作重做范围 | 当前不选；未调用付费生成或复活已停止的全局/参数化重建路线 |

选择依据是局部几何及完整目标覆盖，不是“三角面多，所以必然有问题”。
全源规范网格的 120,000 条边均有两个入射面；本轮没有发现需要用全局焊接补洞来处理的开放边。
这不证明全源的语义连接或几何符合正式 S0。

骨链只读量测：L_Thigh→L_Calf 端点差约 2.729e-5，股骨段长约 0.09511，
两段方向在 rest 相差约 16.46°。L_Upperarm→L_Forearm 端点差约 2.441e-5，
两段方向相差约 16.99°。单位沿用冻结源坐标，不能换称毫米。
这些数值不等于关节中心位于体积内或局部弯曲轴正确；本轮没有用最近顶点距离作此推断。

## 2. 表面量测与明确范围

位置来自冻结 rest 以及 K6 的实际原源 legacy 坐标；没有重新采样 Blender。
面编号沿用 80,000 面规范网格，源 GLB 的全部三角索引经 sourceToProxy 映射逐项核对一致。

| 区域 / 量测 | rest | 关键实际姿态 |
|---|---:|---:|
| 肩臂九面：最小三角高 | 2.8045234e-4 | 跳跃 3.9900971e-5 |
| 肩臂九面：最大“最长边 / 最小高” | 27.7872 | 跳跃 56.9988 |
| 肩臂九面：最小面积 / 同面 rest 面积 | 1 | 跳跃 0.06408 |
| 原膝 1,039 点入射的 2,277 面：最大“最长边 / 最小高” | 37.3291 | 转身 588.5180 |
| 同上：最小面积 / 同面 rest 面积 | 1 | 转身 0.009293 |

这里的细长度是本文明确的诊断定义，**不是正式合同的 face aspect 指标**。
本轮肩臂最小高测的是实际原源，不能和 CHANGE-059 的不同权重/坐标记录混为同一值。
膝入射集合含周边及冻结上下文，上表不是把每个面都判为故障。
数值说明存在退化风险，不独立证明改连接即可消除所有接触。

肩臂八个规范点：
27619、27765、27882、27973、27994、28174、28256、28937。

对应九面：
55006、55261、55262、55395、55511、55624、55745、55746、56511。

九面组成一个边界含五点的拓扑圆盘，但五个关键点还在边界上；
沿真实共享边扩展四圈后为 50 面、38 点、24 点单边界，八个关键点才全部位于内部。
原膝 1,039 点的全部入射面则为 2,277 面、1,237 点，形成一个带状连通区域，
边界分别为 102 和 95 点。它不是一个可随意用平面补上的孔。

**这两个小区域联合只有 2,327 面，1,534 对恢复目标中仅 254 对至少有一面进入选区，
还有 1,280 对完全在外。它们不能单独作为完整修复范围。**

## 3. 选定的施工包络

从原 2,189 个可编辑点的全部 4,903 个入射面出发：

1. 纳入原面 57003、74398，连接原选区在点 14945、33120 处断开的入射扇区，
   消除两处施工边界分叉。只改变选面清单，没有修改源面。
2. 此时边界总边数为 487。全四边补片必须满足
   `4Q = 2E_internal + B`，固定外边界的 B 为奇数时不可能全部使用四边面。
3. 将被选区围住的单个原面 53743 纳入施工包络，消除选区的三点小内边界，
   总边数成为 484。**原模型此处本来就有面，这不是给原模型补洞。**

最终边界完整序列、源接缝副本和坐标哈希见私有 `selected-boundary.json`：

| 项目 | 结果 |
|---|---|
| 原源面包络 | 4,906 / 80,000 面，约 6.13%；逐面 ID 已固定 |
| 规范点 / 源点（含副本） | 2,690 / 3,281 |
| 连通区域 / Euler | 1 / -5 |
| 简单闭合边界 | 7 条，点数 [324,95,19,14,13,12,7]，总计 484 |
| 方向与连接 | 内部边恰有两个反向入射面，边界无分叉 |
| 原 2,189 可编辑点及肩臂八点 | 全部在边界内部 |
| 原右侧/掌指冻结点 | 57 个位于边界，内部为 0；坐标、权重及源接缝必须保持 |
| UV 接缝 | 531 个规范点有不同 UV 的源副本，不能仅按位置焊成一个 UV |
| 全部 1,534 对目标接触 | 每对至少一面进入包络；1,223 对的两面均在包络内 |
| 包络外 | 75,094 个原面保持原连接、坐标、属性与动作行为 |

七条边界围绕的是施工选区及保留岛，不是七个新的人体解剖接口，
也不是 formal S0 的五接口。偶数边界、Euler 和覆盖通过只是必要条件，
**没有证明已存在合格四边布局、关节环或自然变形**。

历史膝比较有 238 组不同源超限面对；包络覆盖其中 235 组。
其余 40351/40707、40533/41787、41684/42530 的所有角点都在原可编辑区外。
旧 `BoundedSkinProblem` 已通过 `editable_face_pairs` 过滤固定面对；
这三组保留为明确的源冻结上下文，不计作本轮修好，也不从历史清单删除。
因此后续局部通过仍不代表右侧或全身通过。

本轮曾检查一个更严格的选择：去掉任何接触冻结点的整个三角面。
它漏掉六组源超限面对，完整覆盖断言被拒绝。原规则冻结的是点，
不能擅自扩大成冻结其所有相邻面；最终回到原入射面范围，且所有冻结点仍处于固定边界。
中间清单只读保留。最终清单还修正了中间报告“两个固定面对”的文字笔误；
原数值列表始终列出上述三个面对。

## 4. 需要保留、重做什么

| 工作 | 局部试修 | 正式生产资产 |
|---|---|---|
| 表面与连接 | 以原源为表面依据，在七条准确边界内直接制作沿压缩/伸展方向的连续关节环与过渡；保留所有边界及区域外身份。不能把现有三角面的简单配对当作已完成面流设计 | 可复用源对应、区域设计和被实际验证的面流依据；仍需满足全身 S0 及身份合同 |
| UV / 材质 | 保留原面角 UV 与接缝对应，重建区建立逐面源对应并核查接缝。当前 GLB 只有纯色材质、没有内嵌 images，但已有 UV 仍需维护 | 后续贴图、法线和闭合装配需独立复验 |
| 骨架 / 权重 | 此次保持 native 41 骨 rest/bind 和五动作，隔离拓扑变量；新点需要一套明确支持、最多四影响且质量有记录的权重。边界原权重保持；不能无记录截断或归一化 | 41→32 不是改名：twist 结构不同，权重、绑定及五动作需重新适配；冠叶控制也未解决 |
| 验收工具 | 原 face ID 在重建区会失效，必须有完整源区域到新面的对应；区域外用原 ID。所有目标继续可追踪，并对实际新表面重查接触/折角，不能删除旧 ID 后报零 | 继续原 S0→闭合身份→用户静态审核→固定 32 关节→生产门禁 |
| 交付 | 仅 vendor capability trial 的隔离候选和报告，不能替换 App 正式资源 | USD/USDZ、RealityKit/macOS 14 与完整动作往返仍须按 ADR-003 验证 |

本次不改变架构、运行时依赖或正式合同。不能依赖运行时 Corrective Smooth 或 morph
来补过 macOS 14 的基础身体完整性，也不复用关闭的 R0、v005 或 no-seam 构造。
人工 DCC 可以是资源选项；当前没有必须移交人工的结论。

## 5. 下一次施工的完整交付与验收

下一阶段直接做带实际静态网格的施工验证，**只交计划或工具、没有网格，按失败结算**。
施工可使用 Blender 4.3.2 的直接 BMesh 编辑，但必须如实记录算法/建模过程，
不能把程序布局冒称手工制作。禁止全局 remesh、以最终 shrinkwrap 代替结构制作或复活已关闭方法。

静态施工先交付：源面→新面的完整对应、七边界不变量、关节连续环/压缩侧与伸展侧布线、
接缝与权重转移规则、同镜头静态图及独立几何审计。
施工前在新登记中冻结与新拓扑对应的判据；不能按输出结果改阈值：

- 区域外及固定边界保持；原右侧/掌指点、源资产和历史文件不变。
- 新区无退化、翻面、重复面、内部非流形或新增静态自交。
  关节核心和相邻环的极点、连续 quad strip、纵向方向及面比例按现有权威定义检查；
  全部必要声明必须能从真实 connectivity 验证，不能仅填环点标签。
- rest 的双向表面偏差、局部轮廓、腰部既有造型和接缝逐项比较并审核；
  不能只检查新顶点到旧表面的距离，就声称整个新表面一致。
- 同一套新权重执行 0°、15°、60°、转身、跳跃的实际求值：
  全部 1,534 对目标必须通过源区域对应重新检查；
  重新扫描新表面与全源上下文的潜在新增接触，不能沿用旧面对作唯一候选集合。
- 原受保护区域保留不劣于源的标准，原 1e-6 折角容差不放宽。
  新面的面积/翻折以其自己的 rest 及实际几何计算；旧面与新面的角度不能按数组序号硬配。
  无法建立完整对应或有未定义验收项，静态阶段即拒绝，不带着缺口进入动作阶段。
- 原尺寸视觉仍必须通过：膝部无残留台阶/深折，肩臂无新沟槽/穿插，
  腰部改善保持。不能以平均改善、数值更稳定或几条接触消失代替通过。
- 四姿态全部通过后，冻结网格和权重，再验证未拟合角度、目标邻帧、
  0–60° 逐度和原五动作的既定 305 帧，并做隔离文件往返。

静态报告只对应 vendor trial，不送 formal normalizer，不写 S0 通过或允许生产绑骨。
完成全部局部阶段之后，才决定右侧、全身及正式生产适配；本预算没有承诺完成上线。

## 6. 一次试修、整条路线止损

本次结构比较上限 60 分钟，已于 UTC 2026-09-14 07:42:42 开始；
实际阅读、计算、原图审查、文档和发布都计入。
最终用时以归档后的 `run-close.json` 为准，不预填估计分钟。
结构路线总投入上限 **420 分钟**：本次比较最多 60 分钟，加下一次 B1 最多 360 分钟。
这不是成功工期估计；原权重路线的未用额度已关闭，不转入此预算。

| 阶段 | 上限 | 必须交付 | 失败/超时 |
|---|---:|---|---|
| 本次结构比较 | 60 分钟 | 有依据的选择、完整包络、迁移成本和本表 | 无法选择则停止；本轮已形成选择，无新模型 |
| B1 静态施工 | 120 分钟 | **一份真实局部新网格**、完整对应/验收登记及独立静态审计通过 | 停止 B1，不追加纯分析轮，不开放动作预算 |
| B1 一套权重及实际四姿态 | 180 分钟 | 同一冻结网格、一套权重、全部实际几何和同镜头视觉通过 | 停止，不改第二版网格/权重，不借连续验证时间修补 |
| B1 固定结果的连续性与往返 | 60 分钟，仅上一阶段全部通过 | 连续/邻帧/305 帧及隔离往返完整结果 | 未完成记未通过，不补采样冒充全量 |

B1 最多一个真实源候选。首次对真实源构造新 connectivity 即消耗唯一名额；
工具错误、几何拒绝和超时也计入。审计拒绝后不更改候选再以新编号重跑。
编码、有效行为测试、工具运行、审查与归档全部在各阶段时间内；
确定性施工中的已登记步骤可完成，但不能借单个进程包装无上限候选搜索。

本轮结束时 B1 **0/1，未启动**。后续接续从静态施工开始，
不再安排一轮同样的路线比较。失败时保留完整资产与证据并关闭 B1 自动投入；
届时只根据实际失败决定是否值得更大范围生产重建或引入 DCC 协作，不预设必须人工接手。

## 7. 证据与当前模型状态

私有目录：
`/Users/zhoutong/Library/Application Support/nick/tripo-knee-structure-review-20260914/`。

- `structure-measurements.json`：源/实际姿态的尺寸和面积定义、各圈选面与接缝。
- `coverage-and-bones.json`：完整接触覆盖、骨链量测、不同冻结策略的边界。
- `proposed-boundary.json`：保留中间八边界/奇数边界状态，不作执行输入。
- **`selected-boundary.json`**：唯一施工范围，SHA-256
  `f2e75c38a6430c3c578506808e8cfa09f3ad684fb212f251d29f9786a8cface7`。
- `decision.json` 绑定各报告与原源；`verification.json` 核对历史/用户/App 状态；
  `run-close.json` 在最终推送和远端核验后一次结算用时。

所有测量公式、直接输入哈希及精确 ID 均在报告中。
本次为只读数据查询和方案文档，没有新增工程实现，因此未增加代码测试或重跑 Blender。
旧 1,996 份受保护文件由 before-state 逐项绑定；最终保全以 verification 为准。

**模型结果：0 个新模型、0 次 QP、0 次 Blender。**
当前有效加工源仍是 CHANGE-033 腰部 GLB；冻结 K3 的 169 条实际保护失败没有改变。
本轮进展是完成了有完整范围和止损条件的结构选择，不是模型已经改善。

## 8. B1 施工前校正与固定执行边界

原 4,906 面覆盖了全部接触目标，却没有覆盖完整关节核心及相邻环。
四个固定圆柱以原 L_Calf、L_Thigh、L_Upperarm、L_Forearm 的 head 为中心、
tail-head 为轴，半径/半长度分别为 0.05/0.03、0.06/0.03、0.055/0.035、0.04/0.025（原源坐标单位）。
原范围漏掉核心面中心：膝 0、髋 38、肩 449、肘 72。若仍冻结周边，则无法完整施工/验收。

在首次源 connectivity 构造前，用核心点及其真实一圈邻点的全部入射原面补全范围：
5,824 原面、3,174 点、九条无分叉闭边界，长度 [213,170,94,14,13,12,11,7,4]，共 538 边；
右侧/掌指冻结点内部为零，全部 1,534 对接触仍覆盖，74,176 个外部原面保持。
精确 ID 与四个区域已写入新的私有 `tripo-b1-static-20260914/construction-scope.json`。
旧范围与历史记录不覆盖；该校正增加 918 原面，不增加候选、时间或阶段权限。

施工只改变局部 connectivity，固定全部顶点位置，以源面角 UV 约束不跨越原 UV 接缝。
固定执行管线：最多两遍/512 次三角换边预处理；一次最多 300 秒的几何 quad matching；
最多四遍/512 次双四边面换边以疏导核心极点、改善轴向布线。每次局部替换保留完整来源并检查几何。
这是程序化局部编辑，不称手工 DCC；简单配对数量不算面流通过。
独立审计沿用真实环、核心及一圈极点、面比例 3.5、连接方向 0.85 等原判据。
固定 rest 双向表面偏差上限为 0.001 原源坐标单位；采样未能提供足够上界时记未证实，不假定通过。
UV/来源、完整外部上下文、新增自交、静态状态和原尺寸同镜头图一并审核。
无完整环/支撑路线声明即失败，不以权重试修补足静态缺口。

## 9. CHANGE-064：真实网格交付，静态拒绝

B1 从 UTC 2026-09-14 08:39:24 起算，施工结果于 09:15:42 完成，
Blend 物化、重新打开独立审计与十二张原图于 09:21:59 完成；这两个时间都不是最终总工时。
最终归档与推送后的工时由 §10 的 run-close 一次结算，仍在静态阶段 120 分钟预算内。

实际执行 512 次三角换边、一次几何 MILP 配对及 97 次四边面换边。
产生 40,000 点、77,516 面的完整隔离静态资产；重建区域为 2,484 个四边面和 856 个三角面。
没有新增或移动顶点，没有权重 QP、新权重或新动作。几何 MILP 的最优状态仅针对登记的配对目标，
不能解释为面流或源表面几何已通过。

| 独立检查 | 实际结果 | 结论 |
|---|---|---|
| 全点坐标、74,176 外部原面及面角属性、九边界 | 与冻结源逐项一致；完整来源、UV 接缝及 raw normals 保留 | 通过保全检查 |
| 静态状态 | Armature、modifier、vertex group、parent、shape key、action 均为 0 | 通过；仅静态试验资产 |
| 连通/几何 | 1 个闭合组件，无非流形边、退化面或重复面；Euler 0，与保留的全源拓扑对应 | 不是正式 S0 |
| 局部纯四边、面比例 | 残留 856 个三角面，全部局部面的最大最长边/最短边比 12.5971，阈值 3.5 | 未通过 |
| 折叠四边面 | 8 个，采用已有 auditor 的对角三角法线定义 | 未通过 |
| 静态自交（局部对全源上下文） | 原源 0 对，候选 1 对；实际新面行 55779/56320，左肩腋下附近 | 未通过 |
| 源→候选表面距离 | 实测采样最大 0.00183258；90 个变化三角形未得到阈值内覆盖上界 | 未通过 0.001 上限 |
| 候选→源表面距离 | 实测采样最大 0.00202429；484 个变化三角形未得到阈值内覆盖上界 | 未通过 0.001 上限 |
| 原尺寸视觉 | 12 张 1800×1800 同镜头图已检查；裆部新增明显斜向角痕，线框仍不连续 | 不接受 |

距离单位均为冻结源坐标单位；“未认证三角形数”包含已测超限与覆盖上界未收敛，
不等于同样数量的独立几何缺陷。采样最大值已经超限，无需靠不确定上界推断失败。
保持所有顶点不动仍会因连接/剖分变化改变三角形之间的整片表面，这次结果直接体现了该差异。

| 关节 | 合格真实闭环 | 核心及真实一圈极点 | 区域内三角面 | 最大面比例 |
|---|---:|---:|---:|---:|
| 左膝 | 0 | 69 | 45 | 7.9883 |
| 左髋 | 0 | 58 | 24 | 6.4239 |
| 左肩 | 0 | 163 | 98 | 6.9829 |
| 左肘 | 0 | 29 | 7 | 5.4718 |

每个关节都要求至少四个合格环、核心及一圈极点为零，并验证连续四边条带/轴向连接。
本次全部失败，未形成完整解剖支撑路线声明；支撑路线记未认证，不填空值通过。
局部凸性/形状评分也没有充分排除实际非平面四边折叠与全局相交，属于本次施工方法的能力缺口。
审计后冻结候选，没有修改第二版，也没有为处理上述缺口重开构造或求解。

工具验证：25 项 host 测试、5 项 Blender 合成几何测试通过；三个核心构造模块合并覆盖率 85%。
合成圆柱能通过真实环/条带检查，三角化后被拒绝；合法共边、交叉上下文和表面覆盖上界均有测试。
这些结果只说明相应工具行为，不说明真实 Sprout 已修好。IO 编排不包含在该覆盖率口径内。
Blender 共三次：元数据查询、合成测试、真实资产物化/审计/出图各一次；真实源候选和几何 MILP 各一次。

视觉证据只含三个正面固定镜头。材质高亮和全身 wire review 的源/候选共有细线伪影限制部分细节判断，
没有把这些图当全身八方位身份验收，也没有验证任何新姿态的自然变形。

## 10. 关闭 B1、保留结果与后续决策

**B1 静态未通过，1/1 已用；后续权重/四姿态 180 分钟、连续/往返 60 分钟不开放。**
静态阶段未用时间随 B1 关闭，不因还有余额继续调整换边参数、容差、区域或生成第二版。
有效加工源仍为 CHANGE-033 腰部 GLB，冻结 K3 的 169 条实际保护失败保持，正式资源与 S0/32 关节合同不变。

此次应得出的结论是：固定全部点位、以局部换边和三角配对为主的办法，没有完成所需关节布线。
原计划允许在边界内部认真重建面流，而本次实现没有展现这项能力；这是施工方法未达目标，
**不是完整定向重拓扑已经被验证无效，也不是模型整体无解或必须由人工接手的证明。**

下一步先做生产路径决策，当前不再启动一轮模型试修。新路线若值得投入，进入条件是：

1. 给出覆盖肩/肘/髋/膝及腋下/裆部过渡的完整环与支撑布线图，明确内部点的新增/重排策略；
   不能再期待从原随机点位的配对中自动长出完整关节环。
2. 对保留边界、身份表面、UV 和正式 32 关节交付提出一份完整施工与验证方案；
   明确哪些 vendor 41 骨工作可复用、哪些权重和动作需要重做。
3. 再选择具体实施能力：AI 可直接执行的定向建模，或 DCC 协作；以可审查方案和交付能力选，
   不以反复失败推定必须人工。本轮没有联系他人、购买服务或调用付费生成。

新路线尚未登记或授予试修额度，不能借 CHANGE-064 继续执行。允许的接续是上述一次路径决策，
不能将“决策”拆成滚动诊断或又一批配对候选。

私有证据目录：`/Users/zhoutong/Library/Application Support/nick/tripo-b1-static-20260914/`。

- [真实候选 Blend](</Users/zhoutong/Library/Application Support/nick/tripo-b1-static-20260914/candidate.blend>)：
  SHA-256 `844c0522fa9b9b4ccddd5a97a903b9eb5809132538bb7071161949d2f2f167a4`。
- `candidate-mesh.npz`：实际连接、源面完整保守来源、面角 UV/raw normals；
  SHA-256 `cb0390fdfced8725f563913765830a337ff787340963e603a7d85588e3623732`。
- `registration.json`、`construction-scope.json`、`candidate-consumed.json`：事前判据、范围及唯一名额。
- `source-preservation.json`、`topology-and-joints.json`、`static-intersections.json`、`surface-deviation.json`：直接机器证据。
- `render-manifest.json`、`visual-review.json`：十二张原尺寸图的哈希、审查与限制。
- `decision.json`：十项失败/未认证检查及关闭决策；不是 formal normalizer 输入。
- `input-verification.json`：原 2,004 项冻结文件及四份用户文件保持。
- `verification.json`、[run-close.json](</Users/zhoutong/Library/Application Support/nick/tripo-b1-static-20260914/run-close.json>)：
  最终推送、工作树保全及包含归档的总工时；不把前面的工具完成时间当最终结算。

## 11. CHANGE-065：生产路径决策已完成，未形成开工条件

[完整重建决策与 DCC 工单](./pet-character-production-rebuild-decision.md)已列出八关节、肩腋/髋裆分支、
内部点重排、五接口、身份/UV、32 关节迁移和后续全部门禁。选择完整 DCC 直接制作为生产方向；
不再把 Tripo 41 骨的局部试验当成可直接晋级的正式资产。

工单中的解剖布局不是已完成的面连接/点位设计。当前没有实际完整布局、经确认的 DCC 执行能力或
可依据的施工预算，因此 §10 的新施工进入条件尚未满足，AI 自动施工不启动。
建议以能直接制作角色拓扑的 DCC 协作补足能力，我继续负责合同、审计及生产接入；没有联系或购买服务。
这不是证明人工是唯一解，也不再承诺当前 AI 能独立完成尚未展示的建模工作。

本次无新模型/权重/动作，没有新增可采用的改善。B1 及旧预算不重开，下一项实物仍是完整静态网格，
不是追加配对器或另一轮局部诊断。来源文件可用性及旧交接包的过期说明见工单 §7。
