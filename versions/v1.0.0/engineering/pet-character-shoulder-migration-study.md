# 小草肩腋：真实参考量测与局部迁移设计

> CHANGE-066，2026-09-15，@winston（AI 执行）。
> 当前完成：真实身份源绑定、参考曲面量测、有序截面导向及下一次制作边界。
> 尚未完成：真实小草四边补片、完整身体、S0、生产身份或动画。

## 1. 进展与来源

在用户说明无法寻求人类建模协作后，AI 已完成独立合成肩部研究：40° 基例，以及
35° / 宽度 0.25 两个原迁移失败样例的后续局部修复，都通过各自联合质量检查和 Blender 静态审计。
原“直接迁移 0/2”仍是历史事实。实验只有三条声明环、没有生产完整语义，不能升格为真实小草通过。

本轮正式身份 GLB 可用，SHA `d8bfa4d395a97c13839188d9c5d7f2212ae3c6e346262f874eee985f38deca36`。
它与暂时丢失的 Blender Studio 人体基模是不同文件；本轮未恢复或再次下载外部基模。
原始 GLB、正式资源及机器合同不变，合同与 Skill 副本逐字节相等。

仅在独立 Blender 场景提取原始 REST 三角表面，统一高度至 1.10 m，+Z 向上、正面 -Y、左侧 +X。
没有应用 Head=0.90，也没有调整为生产 A-pose；这是一份量测参考，不是获批闭合身份。
93,375 顶点、176,466 三角面、529,398 个面角 UV 保留。直接读取 GLB 缓冲复核时，面索引与 UV 精确一致；
归一化坐标最大差约 0.000394 mm，记录为 Blender 浮点变换差异，不修改生产几何阈值。
旧骨仅用 `LeftArm.head → LeftForeArm.head` 给截面方向提供初始线索；导入的显示尾端不可靠，未参与量测或新骨架设计。

完整证据位于私有 `sprout-shoulder-source-study-20260915/`；App 内归档入口：
`character_pipeline/sprout/v2/work/experiments/sprout-shoulder-source-study-20260915/README.md`。

## 2. 已测清的几何条件

切片原点为 `(0.0465252474, -0.0131218787, 0.4909945726)` m，单位方向为
`(0.7841630433, 0.0579672519, -0.6178415001)`。
它离水平线向下约 **38.15868°**，等价于离竖直向下 **51.84132°**；不能把前者当成生产约 40°–45° 的躯干外展角。

| 截面轴向位置 s | 实测周长 | 前后 / 上下尺寸 | 32 点时平均周向边长 | 设计用途 |
|---|---:|---:|---:|---|
| 0.160 m | 346.25 mm | 125.98 / 86.89 mm | 10.82 mm | 拟用环 1 导向 |
| 0.170 m | 354.79 mm | 129.24 / 89.00 mm | 11.09 mm | 拟用环 2 导向 |
| 0.180 m | 362.87 mm | 132.17 / 91.09 mm | 11.34 mm | 拟用环 3 导向 |
| 0.190 m | 370.58 mm | 134.92 / 93.18 mm | 11.58 mm | 拟用环 4 导向 |
| 0.200 m | 378.20 mm | 137.67 / 95.24 mm | 11.82 mm | 拟用远端边界导向 |

以上均为原三角表面与平面的真实交线，并分别排除了同一平面切出的头/躯干/腿部曲线。
每站恰有一个符合 `X > 0.04 m、0.30 < Z < 0.48 m` 的完整轮廓。
有序 32 点导向保存在 `arm-sections/section-guides-32.json`：从最前方交线点开始，沿截面轴正绕序、等弧长取点。
这些点仍是**导向曲线，不是已构造网格上的结构环**，四站也尚未由生产关节区域审计认定为 shoulder rings。

四环间隔 10 mm，平均周向边约 10.8–11.6 mm，因此先选 32 点有实测尺寸依据。
截面长短轴比约 1.45，向远端略增粗；圆截面及解析样例的渐细假设都不适用。
更近肩根的 `s=0.08/0.10/0.12/0.14 m` 均没有独立手臂闭环，实际交线仍延伸到躯干。
原始失败切片完整保留，未把躯干轮廓裁成“肩环”。下一步必须制作肩上盖和腋下的连续过渡。

参考窗口固定为 `X=[0.035,0.225]、Y=[-0.13,0.13]、Z=[0.32,0.47] m`，
以三角形全部三点均在盒内选取 **8,545 面 / 4,653 个导入顶点**，并保存 564 个真实邻点一圈保护面。
坐标相同的 UV 接缝只在分析索引中合并，得到 4,408 个物理点、Euler 0、两条 164 / 107 点简单边界。
源文件和 UV 没有焊接或编辑。边界总边数 271 为奇数，不能把原边界全部锁死再声称能纯四边封接；
由四边面边数计数 `4F=2E内部+E边界` 即可排除该方案。这个盒边界也不是生产五接口。

## 3. 五接口与身份保护

五接口只从冻结的 `retopology-v008-no-seam-body-proxy-v001-report.json` 的 `targetBoundaries` 读取。
报告 SHA `b91698ef881632b52d7d90287096862bea1dd24eec8e190e0509a7fd4a84bf67`；
neck 64、左右腕踝各 32，点序与坐标原样归档，失败报告的 body connectivity 不复用。

接口到本轮原始静止参考的最大最近三角面距离分别约为：颈 45.25 mm、左腕 33.40 mm、
右腕 33.52 mm、左踝 20.65 mm、右踝 18.68 mm。
这些是不同制作阶段之间的对照值，**不是候选误差、接口失效或修改接口的依据**。
原始参考尚未做获批头比例/姿态适配；本次局部补片不得将正式接口整体投射回源表面。

原脸、头、叶冠、掌指、脚趾、右侧及源窗口外全部只读；不复制旧 24 骨权重/动作。
32 点远端试验边界必须使用独立名称，不能冒充正式 32 点腕口。
新补片只作 `scoped_wip`，不装回正式身体、不执行 candidate decision/normalizer、不进入 rig。

## 4. 下一次直接制作任务与预算

**目标：一份贴近上述真实肩腋参考的无骨架四边补片及独立审计。**
新方法先在真实源三角面上建立明确、可检查的局部曲面坐标，再依据实测导向直接布点。
它不调用旧 builder，不把解析圆柱/分支管生成后整体 shrinkwrap 回身份，也不沿 B1 原三角点位换边。

执行顺序固定如下：

1. **源曲面坐标与边界。** 在只读窗口及已保存一圈内，标出前胸、后背、肩上盖和腋底。
   采用带 source face ID 与重心坐标的局部二维参数域；先验证一一对应、UV 三角有向面积同号，
   禁止跨图跳到头、躯干另一侧或其他壳。重新声明隔离补片近端有序边界，拟用 64 点；
   远端固定使用 s=0.20 的 32 点导向。源窗口的 164/107 点边界不直接照搬成新 mesh 边界。
   边界实际坐标、角点与 source 对应须在构造前一次写入 manifest；无法在窗口内闭合就停止此布局。
2. **显式面连接与布点。** 四个拟用关节环各 32 点，以 `(R[k,i], R[k,i+1], R[k+1,i+1], R[k+1,i])`
   连接；向近端的密度过渡、肩上盖和腋下各两条不相交路线必须列出真实 face/edge/vertex ID。
   64→32 密度转换及所有极点只放在胸背稳定面，不能侵入关节、支撑路线及各自真实一圈。
   每个新点通过已绑定参数域取 source 对应，允许内部点重排；禁止全局最近面贴回或重置边界。
   需要实际验证过渡连接和三维点位；本文件不把环带公式当作已完成的全部布局。
3. **静态验证与有界修正。** 首版先保存，再做真实 mesh 审计；只允许一次同域修正，
   参数域、边界、点数和验收标准固定。曲面最近距离仅用于独立量测，不能作为跨壳移动操作。
   修正后的 float32 几何重新审计；若机器和原尺寸观感都通过，才讨论扩大到完整身体。

下一次局部制作总主动工作上限 **90 分钟**，包含布局、代码、测试、审计、原图和归档；
最多 **一份首版 + 一次同域修正**。源参数域及完整连接设计须在前 30 分钟内成立；不成立则按失败布局结项，
不接着换边界或另开参数搜索。若需要局部求解，两次共用最多 200 迭代 / 300 秒；不用求解也照常记账。
最多两次实际 Blender 静态审计，成功时可另做一次精确重放验证；重放不得优化或产生新版本。
这是后续独立真实肩部研究的预算，不继承或重开旧路线额度。本轮未消耗该制作预算。

| 验收项 | 下一次局部要求 |
|---|---|
| 真实面图 | 单连通、两条声明边界、全四边；两边界点序/坐标精确；不靠叠层连接 |
| 几何 | 自交、双对角线折面、内部非流形、退化/重复面均为零；最大边长比 ≤3.5 |
| 结构 | 四条完整环、连续四边条带、connector alignment ≥0.85；肩顶/腋下各两条实际不相交路线；核心/路线与真实一圈无极点 |
| 曲面匹配 | 双向有限采样距离 P95 ≤1 mm、最大 ≤2 mm，顶点到绑定源面 ≤0.5 mm；补片样本含点、边中点、面中心及每面 4×4 内部格点；源域使用全部三角面中心和边中点，实际对应源域在构造前绑定；不声称连续曲面误差界 |
| 面角 | 记录全量最小值与 <30° 面清单；30° 是合成研究目标，正式合同仅保留 10° 诊断值，不暗改成生产硬门槛 |
| 原图 | 前、后、两侧三分之四的肩腋近景；不能有新凹痕、硬脊、异常鼓包或明显轮廓漂移 |
| 保全 | 源、五接口、正式合同、正式资源与本轮导向不变；源码与 Blend 均绑定 hash |

上述拟议环位靠近旧肘位置，旧骨又未通过生产认证，因此必须根据实际新肩区域检查环位语义和空间，
不能仅因有四个闭环就强称 shoulder gate 通过。若固定范围容不下完整关节/路线净空，应明确拒绝该局部设计，
不能用扩大语义排除区的方式通过。

## 5. 后续生产路径与未解决项

推进顺序保持：**真实肩腋局部实物 → 全身八关节/双分支完整设计与 S0 → 闭合身份/用户审核 → 32 骨及动作 → 运行时往返**。
局部失败时保留结果并说明哪一条几何或布局条件未成立，不宣称整体无解，也不默认交给人类。
只有实物通过才扩大投入，不能用越来越多合成参数样例替代真实角色。

本次参数域直接制作计划不是 `licensed_basemesh_adapted` 的来源证明。
正式候选目前仍受该来源合同约束；如未来采用独立直接构造拓扑，必须在正式候选构造前明确更新来源决策/合同及审批。
不以 `scoped_wip` 通过偷偷晋级，也不把缺失的外部源文件描述成已经恢复。
完整身体布局、实际 A-pose/头比例适配、闭合身份 assessor、UV 装配和生产变形均仍待完成。

本轮九项截面工具测试通过；保存三次只读 Blender 进程记录，其中首次捕获因 importer 的显示 Icosphere 被误计入源 mesh 而中止，
第二次只读诊断定位，第三次正确选择带 Armature modifier 的 char1，完成参考保存和三张原图。
没有真实新补片、优化调用或生产角色审计；既有九轮 419 份私有证据复核不变。
